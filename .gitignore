# ---- IDE ----
.idea/
*.iml
.vscode/

# ---- 第三方文献原文与译文：不进入公开仓库 ----
# 2026-09-25 决定。理由：《03-精选文献库（22篇）.md》里已有 22 篇的完整题录（作者、年份、会议/期刊、
# 池内编号与参考文献序号的映射），需要原文时按题录检索即可；公开仓库不必再分发出版商的版权文件。
# 本地工作区仍保留这两个目录，只是不纳入版本控制。想加回仓库时删掉下面两行即可。
阶段02-文献调研与开题/文献PDF/
阶段02-文献调研与开题/文献翻译/

# ---- 数据集：第 5 阶段封版时定，不入公开仓库 ----
# 2026-09-25 决定。数据集 v1.0 收录的是采集到的上市公司公告、监管公开信息、政策文件
# 与正规财经新闻正文——其中新闻与公告正文是第三方文本，公开仓库不再分发。
# 数据集可复现：按 `代码\数据准备\README.md` 的接口约定即可用同一套脚本重新生成
# （`python 代码\数据准备\run_all.py --profile v1`）。本地工作区仍保留该目录。
# 想加回仓库时删掉下面这行即可。
阶段05-数据准备/数据集/

# ---- 第 5 阶段小规模试跑目录：不是交付物 ----
# 《12-第5阶段任务书（数据准备）》§九 要求在 2～3 家公司／10～20 篇上先整体跑通一遍。
# 试跑结果只用于验证管线，与正式数据集 v1.0 物理隔离，不入仓库。
阶段05-数据准备/_试跑/

# ---- 第 6 阶段小规模试跑目录：不是交付物 ----
# 同《12》第9节 的「小规模先行」纪律：第 6 阶段的抽取管线也先在少量文档上跑通再放量，
# 试跑结果只用于验证管线、不入仓库。
阶段06-事件抽取与知识图谱/_试跑/
# 抽取组件的试跑产物同样不是交付物：T1 的小规模先行走在这里。
代码/抽取与图谱/_试跑/
# 抽取缓存在 数据集\_抽取缓存\ 之下，已被上面的「数据集」规则整体覆盖。

# ---- Python ----
__pycache__/
*.py[cod]
.venv/
venv/

# ---- 密钥与本地配置 ----
.env
.env.*
*.local.json
config.local.*
