# 基于事件知识图谱与RAG的A股财经信息智能问答系统设计与实现 —— 项目执行总控文档 v2.8

## 一、文档信息

| 项目 | 内容 |
| --- | --- |
| 文档名称 | 项目执行总控文档 |
| 版本 | v2.8（执行用，持续更新） |
| 状态 | 执行中 |
| 对应阶段 | 第 1 阶段（选题与项目规划）收尾，同时作为第 2 至第 12 阶段的执行依据 |
| 项目类型 | 软件工程本科毕业设计 |
| 文档作用 | 作为本人执行本课题的总控文件，锁定最终题目、项目背景与意义、研究目标、研究问题、研究假设、研究内容、项目边界、用户角色与功能需求、非功能需求、系统总体架构、数据设计、知识图谱本体、时间维度、技术路线、混合检索策略、实验方案、评价指标、论文目录、进度安排、数据推进策略、风险应对与交付物清单，作为后续各阶段唯一的执行依据 |
| 基线文档 | 《项目总体计划书》v1.0（2026-09-22，共 803 行，其仍然有效的内容已全部并入本文件，v1.0 不再单独保留）；本文件在其基础上完整继承仍然有效的内容，先后完成 v2.0、v2.1、v2.2、v2.3、v2.4、v2.5、v2.6、v2.7 与 v2.8 升级 |
| 配套文档 | 《项目总体方案（导师审阅版）》 |
| 学生姓名 | TBD |
| 学号 | TBD |
| 学校与学院 | TBD（按学校下发的模板填写） |
| 指导教师 | TBD |
| 编制日期 | 2026-09-22 |
| 最近修订 | 2026-09-26（v2.8：第 6 阶段（事件抽取与知识图谱）收口——按《15-第6阶段任务书（事件抽取与知识图谱）》第2.4节（二）在 第12.4节 追加一条注记，说明抽取所用的模型与 Prompt 版本属上游数据生产参数、不并入本表的七项，七项的计数与取值均不变；上一版 v2.7 为 2026-09-25 的前五阶段审核后的问题修复） |

### 1.1 文档定位与分工

本文件是自己执行用的总控文件，不是交给导师的版本，也不用于替代开题报告与论文正文。它与《项目总体方案（导师审阅版）》构成一组分工明确、内容互相一致的文档，两者定位如下。

《项目总体方案（导师审阅版）》用于导师审阅与方向确认，只包含题目、背景、目标、研究内容、功能、技术路线、特色、可行性与进度，这些内容用于让导师快速判断选题是否成立、边界是否清晰、工作量是否合适，因此篇幅短、结论性强。

本《项目执行总控文档》用于自己执行，除上述内容外，还包含数据模型、图谱本体（实体类型、核心事件类型、核心关系、事件属性、实体消歧与事件去重）、时间维度设计、混合检索策略、实验方案（测试集、抽取评测集、实验配置、消融实验、评价指标定义与判定规则）、数据推进策略、风险应对与交付物的全部细节，是每天动手实现、实验与写作时真正查阅的文档。

两份文档共享同一套题目、研究目标与功能范围，不应出现互相矛盾的表述。若导师评审或学校要求带来方向性调整，先更新《项目总体方案（导师审阅版）》，再同步更新本文件并在修订记录中登记版本与原因；若只是实现细节的调整，例如字段微调、索引参数变化或测试集题量按比例缩减，只在本文件的修订记录中登记即可。

### 1.2 修订记录

| 版本 | 日期 | 说明 |
| --- | --- | --- |
| v1.0 | 2026-09-22 | 首次编制《项目总体计划书》，状态为待导师确认，锁定题目、项目边界、功能范围、技术栈、初步数据设计、实验方案、论文目录与进度安排 |
| v2.0 | 2026-09-22 | 依据评审意见修订，完成文档分层、研究假设补充、登录范围决策改写、非功能需求口径调整、数据模型收敛为六表、实体类型收敛为 6 类、核心关系英文命名与去重、事件属性瘦身与时间归一化、实体消歧与事件去重补充、FAISS 术语纠正、时间维度与数据截止时间重写、一次提问完整流程新增、实验方案整体重写、主要工作与特色改写、论文目录调整、数据推进策略与风险应对补充 |
| v2.1 | 2026-09-23 | 依据第二次评审修订：研究假设改为陈述式、事件类型固定 8 种、路径深度改为问题金标准、测试集拆分 108+12、消融 D/E 明确继承关系、检索指标统一 chunk 级、新增上下文预算、评分改为稳定性口径、图谱证据闭环（除 EVIDENCED_BY 外其余 8 条关系统一携带证据属性）、事件参与模型重构、时间模型与数据截止时间改为数据集版本级、新增 session_id、数据管理改为后台能力、数据合规与评测措辞调整 |
| v2.2 | 2026-09-23 | 依据第四次评审修订与第 3 阶段（需求分析）的跨文档一致性复核：**证据属性口径残余**统一（第6.2节 功能 2 原写"9 条核心关系都需要记录来源文档编号、来源文本块编号与置信度"，第13节 原写"图谱路径中的每条关系…关系上的 source_doc_id 与 source_chunk_id"，两处均与 第9.2.3节 冲突，已按"除 EVIDENCED_BY 外"收口）；**系统指标口径归属**更正（第12.7节 原称全部沿用第 7 章，实为第 7 章只规定了响应时间的记录口径与"10 个并发成功率不低于 95%"，而"连续 100 次请求成功率不低于 95%"是 第12.7节 新增的补充口径）；第5.1节 与 第8.4节 补充排除 **Agent、多模态与语音、实时行情**（《05》第五节曾称已写明，实际未见）；第8.2节 数据知识层清单按 6 类实体补齐并去除"时间"混列；第12.1节 的"单跳事实型问题"改为"事实型问题"（0 跳与 1 跳严格区分）；第12.8节 的三个分析子集补上按 第12.2节 标签给出的定义并说明允许重叠；第7节 补充"文件格式错误"场景的格式清单落点；第7节 具体数值目标的表述收窄为"响应时间绝对值"；第16.1节 与 第16.3节 的 RAG 阶段名统一为"RAG检索系统（普通 RAG）"；第一节 文档信息表的基线文档条目说明 v1.0 不再单独保留 |
| v2.3 | 2026-09-23 | 依据第 4 阶段开工前的最后一轮设计口径审查：**Method 定义收口**（原 第12.5节 只写"Vector RAG + Event KG"，未说明图谱深度与是否启用时间过滤／证据排序，导致 Baseline 2 与 Method 的对比无法唯一确定；现明确 **Method ＝ 12.6 节的 C 组 ＝ Vector RAG + 2-hop Event KG，不启用时间过滤、不启用 E 组证据排序**，使该对比的唯一变量恰为"是否使用事件知识图谱"，并补上 Baseline 2 ＝ A 组的对应关系）；**Top-K 证据集合形成契约**（原 第12.4节 把 Top-K 定义为"向量检索返回的候选数量"，而 第12.7节 的四项检索指标又在"Top-K"上计算，两者口径脱节：现给出五步契约——两路候选统一映射 chunk_id、按 chunk_id 去重（同一文本块只算一个证据）、按固定规则裁剪、**在 D／E 分组排序之前**保留到 K 个、最终证据集合即指标计算对象与送入模型的证据上下文；第12.4节 的 Top-K 行同步改为"最终证据集合的文本块数量上限"）；**user 表改为固定建立**（原写"可选表，第一版可以不建立"，与"固定六张表"冲突；现改为六张表全部建立，第一版不启用登录业务、user 表保持为空）；**H2／H3 措辞收紧**（H2 的"图谱路径深度"改为"系统检索深度"，与 第12.2节 的 gold_hop_depth 彻底分术语；H3 补上"系统检索深度固定为 2-hop"这一条件，使 C→D 的唯一变量在假设本身即可读出）；**RQ3 预期产出改为观察性结论**（删去"必须加入时间过滤"这类本课题数据规模不足以支撑的规范性表述）；第1.3节 第 7 条与本节的 user 表口径同步；第1.3节 标题补注 v2.2／v2.3 的改动位置 |
| v2.4 | 2026-09-23 | **第 4 阶段（系统总体设计）开工登记**：第 3 阶段交付物《07-需求分析（第三阶段）》经本人确认，其 FR-01～FR-06、UC-01～UC-07、NFR-01～NFR-06 的编号与验收口径冻结，第 4 阶段的进入条件解除；第 4 阶段产出 `10-系统总体设计（第四阶段）.md`，对应论文第四章 4.1～4.7。本节登记的是**阶段状态与验收依据的变更**，不改变 第5节～第13节 的任何口径：架构层次、模块划分、六张表、本体（6 类实体／8 种核心事件类型／9 条核心关系）、时间维度、技术路线、实验方案（含 Method ＝ C 组、Baseline 2 ＝ A 组与 第12.7节 的 Top-K 五步契约）、评价指标与论文目录全部沿用 v2.3 |
| v2.5 | 2026-09-23 | **第 4 阶段产出《10》经第二轮外部复审后的口径澄清**（不改变任何已冻结的实验设计与指标定义，只把两处原本可被误读的先后关系写明）：**① 时间过滤与 Top-K 截取的先后关系**（第11.3节 与 第12.7节）：时间过滤作用于**候选集合的成员**，必须发生在'两路候选合并与去重'之前——若放在'保留到 K 个文本块'之后，C 组与 D 组的最终证据集合将恒等，四项检索指标也随之恒等，**C→D 的对比（H3 的检验）将失去可观察差异**；第12.7节 第四步补写分工说明：第一步保证 **C 与 D 可以不同**（H3 可测的前提），第四步保证 **D 与 E 必然相同**（E 组单变量的前提），两条约束作用在不同组对上、互不冲突。**② 候选不足 K 的边界**（第12.7节）：明确'候选证据不足 K 的题目在测试集构建阶段调整'**只适用于测试集构建阶段**；题目一旦入库，不得因实验时实际候选数少于 K 而删题，空缺位置记为未命中并按原指标定义计算，以防止'系统没搜到就删题'的选择偏差。两处澄清已同步写入《10》第4.3节、第4.6节 与《11-第二轮复审判定与修订决议》，属于对既有条款的解释性补充，不构成新的实验变量 |
| v2.6 | 2026-09-25 | **第 5 阶段（数据准备）完成登记与三项 TBD 固化**：第 5 阶段按《12-第5阶段任务书（数据准备）》第九节 的 T1→T9 执行完毕，产出《13-数据准备（第五阶段）》与数据集 v1.0（10 家公司／99 篇文档／707 个文本块／707 条向量，时间覆盖 2026-06-17～2026-09-25）。本节据 第12.4节 自身「选型后固化」的要求，把三项到期的 TBD 落定：**① Embedding 模型 ＝ `BAAI/bge-small-zh-v1.5`（HuggingFace revision `7999e1d3359715c523056ef9478215996d62a620`，512 维，L2 归一化，FAISS `IndexFlatIP`）**——向量索引一经建立，换模型就要全量重建索引，且所有对比组与消融组必须共用同一模型，故不能拖到第 7 阶段；**② dataset_version ＝ v1.0、data_cutoff_time ＝ 2026-09-25**（数据集版本级属性，不属于任何表，与 第二节、第10.3节 的占位口径一致）；**③ 切分参数 ＝ 目标 400／上限 512／下限 128／重叠 50 字符**（封版后不得更改，改参数等于换数据集版本）。**大语言模型与版本、Top-K（K）、Context Token Budget 继续保持 TBD**，分别归「智能问答系统」与「RAG检索系统」阶段固化，本阶段只登记归属、不写死。另修正一处版本号漂移：文档信息表的版本字段与标题自 v2.3 起未随 第1.2节 的 v2.4／v2.5 记录同步更新，本次一并更正为 v2.6。本次登记不改变 第5节～第13节 的任何其他口径：架构层次、模块划分、六张表、本体（6 类实体／8 种核心事件类型／9 条核心关系）、时间维度、技术路线、实验方案（Method ＝ C 组、Baseline 2 ＝ A 组、Top-K 五步契约）、评价指标与论文目录全部沿用 v2.5 |
| v2.7 | 2026-09-25 | **前五阶段审核后的问题修复**（四条，均为审核实测确认，不改动任何实验设计与指标定义）：**① `answer.is_graph_extended` 补登记**——《10》第4.4.1节 表中已使用该字段（记录本次回答是否使用了图谱扩展，决定证据区域显示路径还是显式标注），但本节 第9.1节 的 answer 字段清单漏列，形成“《10》声明不新增字段却实际新增”的冲突；现按六张表口径补入 第9.1节，字段既不新增表也不改动已有字段。**② 第12.3节 的“条”口径澄清**——原文只写“标注总量约 250～300 条”“两集合不得重叠，均记录文本编号”，未说明单位是篇还是文本块；若按篇计，250～300 条将要求同等数量的文档，与 第17节 的第一版 100 篇／第二版 500 篇无法自洽。现明确“条”的单位是**文本块**（《02》第12.7节 已把文本块定为证据的最小单位）。**③ 第12.7节 第一步补写时间过滤**——v2.5 的澄清写明“时间过滤作用于候选集合的成员、必须发生在两路候选合并与去重之前”，但五步契约的**第一步正文**没有写这一步，导致第四步引用“第一步的时间过滤”时需对照 第11.3节 才能读懂；现把该步补进第一步正文，使五步契约自洽。**④ 第 8 阶段名称统一**——第16.1节 写“智能问答系统”、第16.3节 的进度表写“KG-RAG”，同一套阶段表出现两个名字（v2.2 只统一了第 7 阶段）；现把进度表的阶段名改为与 第16.1节 一致。注意：**“KG-RAG”作为方法／架构名称（第11.2节 等）全部保留不变**，本文只统一“阶段名”。本次登记不改变 第5节～第13节 的其他任何口径 |
| v2.8 | 2026-09-26 | **第 6 阶段（事件抽取与知识图谱）收口登记：回 第12.4节 追加一条注记**（依据《15-第6阶段任务书（事件抽取与知识图谱）》第2.4节（二））。注记内容：**抽取所用的模型、模型版本与 Prompt 版本属于上游数据生产参数**——它们决定“图谱里有什么”，不决定“答案怎么生成”，因此**不并入 第12.4节 的七项**；本表的「大语言模型」一行仍指**答案生成模型**、仍在第 8 阶段固化，保持 TBD。**本条注记不改变七项的计数，也不改动七项中任何一行的取值**；抽取侧三项参数的冻结值与选型依据登记在《16-事件抽取与知识图谱（第六阶段）》第一节。除该注记外，本次登记不改变 第5节～第13节 的其他任何口径：架构层次、模块划分、六张表、本体（6 类实体／8 种核心事件类型／9 条核心关系）、时间维度、技术路线、实验方案（Method ＝ C 组、Baseline 2 ＝ A 组、Top-K 五步契约）、评价指标与论文目录全部沿用 v2.7 |

### 1.3 v2.1 关键修订要点清单（v2.2 与 v2.3 的改动见 第1.2节 修订记录）

1. 文档分层：本文件定位为自己执行用的总控文件，负责数据模型、本体、实验与指标的全部细节；《项目总体方案（导师审阅版）》只含题目、背景、目标、内容、功能、技术路线、特色、可行性与进度，用于导师审阅。
2. 测试集多标签化：每道题打任务类型、路径深度（gold_hop_depth，问题金标准属性）、时间约束三个标签，完整版 120 题（核心 108 + 压力 12）、缩减版 60 题（核心 54 + 压力 6），替代 v1.0 的六分类表，避免分组统计口径歧义。
3. 新增抽取评测集：从最终数据集中抽取 Dev 50～100 条（建议 60）与 Test 200 条财经文本人工标注（合计约 250～300 条），Dev 用于抽取规则与 Prompt 的迭代调优，Test 在规则与 Prompt 冻结后一次性评测、只测一次，计算实体、事件、关系、时间的 Precision、Recall 与 F1，用于验证 RQ1。
4. 公司引入股票代码：Company 节点以 stock_code 作为稳定业务唯一标识，公司名称、简称、别名与历史名称只作为属性，名称变化与同名问题不再动摇图谱中的实体身份。
5. 事件时间归一化与证据统一挂载：publish_time、source、url 归属 document，一个事件通过 EVIDENCED_BY 关联多份文档；除 EVIDENCED_BY 外，其余 8 条核心语义关系统一携带 source_doc_id、source_chunk_id 与 confidence 证据属性，图谱从"关系图"变为"有证据的关系图"。
6. 实体消歧与事件去重：实体对齐以 stock_code 为锚，事件按类型、参与主体、时间窗口与触发词相似度判定并合并，合并后保留全部证据文档。
7. 数据模型收敛为六表：user、document、document_chunk、question、answer、answer_evidence（六张表**全部建立**，其中 user 表在第一版不启用登录业务时保持为空、不写入数据），删除 history 表，历史记录通过 question 与 answer 关联查询获得。
8. FAISS 术语纠正：全文不再把 FAISS 称为"向量数据库"，统一表述为"向量索引／向量检索组件"，并说明 FAISS 本质是向量相似度检索库，与 Milvus、Qdrant、Weaviate 这类独立向量数据库系统不同。
9. 新增数据截止时间：data_cutoff_time 与 dataset_version 成对记录在数据集元信息（配置文件 ＋ 数据集说明文档）中，不属于任何表；每篇文档在入库时记录 ingest_time；界面固定显示"数据截至：YYYY-MM-DD"，"最新／最近／近期"等相对时间一律以 event_time 判定、以数据截止时间为基准解释。
10. 同步要求降级：数据一致性不再要求实时同步，改为文档变更后触发重新处理、更新 Neo4j、必要时重建或增量更新向量索引，并提供一致性检查手段。
11. 新增研究假设 H1～H3：三条假设均由原来的提问式表述改写为陈述式、可检验、不预设方向的比较命题，并给出 RQ → H → 实验 → 指标 → 结论的对应关系。
12. 消融实验澄清：明确 1-hop 与 2-hop 的含义（2-hop 包含 1-hop 的结果），并明确 D = C + 时间过滤、E = D + 证据排序，即 C ⊆ D ⊆ E；A～D 组对应 RQ2 与 RQ3（H3 由 C 与 D 的对比回答），E 组单独列为"进一步优化实验"。
13. 评价指标给出定义：检索指标统一为 chunk 级主口径，Recall@K、Precision@K、MRR、Complete Evidence Recall@K 以及 Answer Accuracy、Completeness、Faithfulness 的计算与评分口径全部写明，并规定对不少于 20% 的样本间隔 3 天以上二次复评，报告前后评分一致率以检验评分稳定性。
14. 实验模型固定：新增实验配置固定表（七项），固定 Embedding 模型、大语言模型、模型版本、temperature（固定为 0）、Prompt 版本（v1.0）、Top-K 与 Context Token Budget（上下文预算，预实验后固化）；系统支持多模型切换，但实验期间必须固定同一套配置与同一套上下文裁剪规则，模型名称与版本在选型后固化，当前填 TBD。

本文件对应毕业设计流程中的第 1 阶段，即选题与项目规划阶段。这一阶段要回答三个问题：到底要做什么、做到什么程度、不做什么。题目、边界、功能范围、技术栈、数据模型与实验方案在本文件中一次确定，第 2 阶段之后的需求分析、系统设计、数据准备、知识图谱构建、RAG 实现、系统集成、测试与实验、论文撰写均以本文件为基准。若后续需要调整题目或范围，应在本文件的修订记录中登记新版本并说明变更原因。

## 二、项目概况

| 项目 | 内容 |
| --- | --- |
| 中文题目 | 基于事件知识图谱与RAG的A股财经信息智能问答系统设计与实现 |
| 英文题目 | Design and Implementation of an Intelligent Question Answering System for A-share Financial Information Based on Event Knowledge Graph and RAG |
| 项目类型 | 软件工程本科毕业设计（系统设计与实现类） |
| 关键词 | 事件知识图谱；检索增强生成（RAG）；A股财经信息；智能问答系统；混合检索；知识图谱可视化；证据追溯 |
| 数据来源范围 | 上市公司公告、监管机构公开信息、政策文件与正规财经新闻；第一版不引入股吧数据 |
| 数据规模目标 | 第一版 10 家公司 / 100 篇文本；第二版 50 家公司 / 500 篇文本；第三版 100+ 家公司 / 1000+ 篇文本。第三版规模是努力目标而非硬性验收指标，验收以数据质量、问题覆盖与实验可复现为准（详见第十七节） |
| 数据截止时间 | 数据集版本级属性 data_cutoff_time，与 dataset_version 成对记录在数据集元信息中（配置文件 ＋ 数据集说明文档），不属于任何表；界面固定显示"数据截至：YYYY-MM-DD"，第一版为 2026-09-25（数据集 v1.0 封版时固化，见《13-数据准备（第五阶段）》） |
| 核心数字口径 | 6 类实体、8 种事件类型、9 条核心关系、测试集 120 题（核心 108 + 压力 12；缩减版 60 题＝核心 54 + 压力 6）、抽取评测集 Dev 50～100 条（建议 60）+ Test 200 条（合计约 250～300 条）、消融实验 A～E、对比设置 Baseline 1／Baseline 2／Method、四个里程碑 |

一句话介绍：本课题面向 A 股财经信息的查询与问答场景，设计并实现一个基于事件知识图谱与 RAG 的智能问答系统，把分散的上市公司公告、财经新闻与政策文本组织成可检索、可追溯的结构化知识与文本证据，使用户能够用自然语言提问并获得带有来源的回答。

答辩可直接使用的一句话介绍（最终版）：本课题针对传统财经 RAG 在多实体、多事件关系查询中容易出现信息遗漏的问题，设计并实现一个基于事件知识图谱与 RAG 的 A 股财经信息智能问答系统，通过融合文本证据与图谱关系，为用户提供具有来源追溯能力的财经信息问答服务，并通过对比实验验证知识图谱增强检索的实际效果。

需要强调的是，本系统的定位不是"万能金融 AI"，而是边界明确的 A 股财经信息智能问答软件系统：以事件知识图谱组织实体、事件、关系与时间信息，以 RAG 检索文本证据，最终输出可追溯的问答结果。系统不是实时行情数据库，所有"最新／最近／近期"类问题都以 event_time 判定、以数据截止时间为基准回答，这一点在界面、提示词与论文中都保持一致。

## 三、项目背景与意义

财经信息本身具有分散、更新快、表述专业的特点。同一家公司的经营情况可能分别出现在公告、财经新闻与政策文件中，用户想要回答"某公司最近发生了什么重大事件""某公司的供应商有哪些""某公司在某时间之后涉及哪些政策相关事件"这类问题，往往需要跨越多篇文本，人工完成实体对齐、关系串联与时间核对。直接使用大语言模型回答这类问题时存在明显的不足：一方面模型的训练语料无法覆盖最新的公告与新闻，另一方面模型给出的结论缺少信息来源，用户无法判断答案是否可以采信，也无法进一步查看原始材料。

本课题不做"大而空的万能金融 AI"。按照本科毕业设计的周期与单人开发条件，如果声称要完成一整套投研系统，就需要同时覆盖行情、财务、行业、风险、交易等多个领域，每个领域都只能停留在很浅的层面，既无法形成完整的软件工程闭环，也没有条件用实验证明系统的有效性。而本系统实际上并不需要这些能力，它真正要解决的问题是：把分散的财经信息组织起来，并让用户能够通过自然语言快速查询和关联分析。因此，研究对象被收窄为"A 股财经信息智能问答系统"，系统主要解决财经事件信息检索、关联关系查询、多跳信息整合和带证据的问答这四件事。

题目收敛也有明确的工程考虑。原题目"事件驱动金融知识图谱增强RAG的A股财经舆情投研问答系统"中同时出现了"事件驱动、金融知识图谱、增强 RAG、A 股、财经舆情、投研、问答系统"等多个关键词，容易给评审留下"什么都想做、边界没有划清"的印象。其中"投研"一词的外延尤其大，很容易被进一步追问是否涉及股票预测、投资决策、财务分析、行业分析、风险预测与量化交易，而本系统的实际能力并不覆盖这些内容，一旦被追问就必须反复解释，反而削弱了课题的可信度。收敛为"基于事件知识图谱与RAG的A股财经信息智能问答系统设计与实现"之后，核心技术仍然完整保留：事件、知识图谱、RAG、大语言模型与智能问答都在题目之内；同时题目中明确出现"系统设计与实现"，符合软件工程专业的要求，论文可以按照需求分析、系统设计、数据设计、核心模块实现、系统集成、测试与性能评价的完整主线展开；更重要的是主动降低了项目风险，系统不再声称自己是完整的投研系统，从而避免在答辩中被追问涨跌预测、投资买卖建议与量化交易策略等问题。

课题的定位可以概括为三句话。第一，不是做"万能金融 AI"，而是做"A 股财经信息智能问答系统"。第二，不是研究股票涨跌预测，而是研究财经事件关系检索与问答。第三，不是预先断定"知识图谱一定更强"，而是通过实验验证它在什么类型的问题上能够提供额外价值。

从研究意义上看，本课题的工作集中在三个方面。在数据与知识组织方面，把新闻、公告与政策文本中的公司、人物、行业、机构、事件与政策六类实体，以及它们之间的关系与时间信息组织为事件知识图谱，使原本分散的财经信息具备可查询的结构；产品与地点作为可扩展类型登记在本体设计中，第一版不进入抽取与评测工作。在检索方法方面，把向量检索与图谱检索结合，通过关系路径扩展、时间约束与证据排序改善多实体、多事件问题的证据完整性，并通过对比实验与消融实验给出可检验的结论。在工程实践方面，按照软件工程方法完成一个前后端分离、可运行、可演示、可评价的问答系统，覆盖需求分析、总体设计、数据库与本体设计、模块实现、系统测试与实验分析的全部环节。

## 四、研究目标与研究内容

### 4.1 研究目标

本课题旨在设计并实现一个基于事件知识图谱与 RAG 的 A 股财经信息智能问答系统。系统针对财经新闻、上市公司公告及相关政策信息进行数据采集与预处理，通过实体识别、事件抽取、关系抽取和时间信息抽取构建面向财经事件的知识图谱，同时将原始文本进行向量化处理，形成结构化知识与非结构化文本相结合的数据基础。

在用户提出自然语言问题后，系统采用向量检索与知识图谱检索相结合的方式获取相关证据，并通过关系路径扩展、时间约束和证据排序提高检索结果的相关性，最终利用大语言模型生成自然语言回答，并提供相应的信息来源，对使用了图谱扩展的回答同时给出知识图谱路径，实现财经信息问答的可追溯性。

在此基础上，通过普通向量 RAG 与知识图谱增强 RAG 的对比实验，以及不同图谱检索策略的消融实验，分析知识图谱在财经事件多跳问答场景中的实际作用，为知识图谱与 RAG 技术在财经信息智能问答系统中的应用提供实验依据。

### 4.2 研究问题（RQ1/RQ2/RQ3）

#### RQ1：财经事件如何进行结构化表示？

研究点包括财经实体识别、事件识别、关系抽取、时间信息抽取以及事件与证据的关联。要回答的问题是如何把一篇自然语言财报、公告或新闻，转化为由实体、事件、关系、时间与来源构成的机器可查询结构。

预期产出：一是财经领域可用的实体经济类型、事件类型、关系类型与事件属性定义，即知识图谱本体设计；二是从文本到图谱的抽取流程与实现代码；三是可直接用于查询与实验的事件知识图谱。图谱中的实体、事件与关系都必须能够回溯到原始文档，保留来源与时间信息。

以 A 公司为例，其结构化表示形式如下：

```text
A公司
   │
   ├── 所属行业 → 新能源
   │
   ├── 供应商 → B公司
   │
   └── 发生事件 → 监管处罚
                      │
                      ├── 时间
                      ├── 类型
                      ├── 相关机构
                      └── 原始公告
```

#### RQ2：知识图谱能否弥补传统向量RAG的关系检索不足？

研究点是对比"普通向量 RAG"与"向量 RAG + 事件知识图谱"两种方案。重点研究当问题涉及多个实体、多个事件或时间关系时，加入知识图谱后是否能够减少证据遗漏、提高回答准确性。

预期产出：同一测试集上两种方案的对照结果，包括检索指标与问答指标的差异、不同问题类型上的表现差异，以及知识图谱在哪些问题上有效、在哪些问题上没有明显作用的分析。RQ2 是本课题的核心实验问题，论文的实验章节主要围绕它展开。

#### RQ3：不同图谱检索策略对系统效果有什么影响？

研究点是比较不同图谱检索策略，即 0 跳（只进行文本检索）、1 跳（文本检索 + 一跳图谱关系）、2 跳（文本检索 + 两跳图谱关系）与时间过滤（文本检索 + 图谱 + 时间约束）对系统表现的影响。

预期产出：不同策略在检索准确率、证据完整性与最终问答质量三个方面的对比结果，以及在消融实验基础上给出的**观察性**结论——在本数据集与固定实验配置下，哪些问题类型对不同检索深度更敏感、时间过滤在哪些条件下表现出额外作用。这里不写成"必须加入时间过滤"一类的规范性建议，因为 108 道核心题与所设定的实验配置只支持"在本数据集与固定配置下观察到"这一强度的结论，不支持规范性判断。响应时间不列入 RQ3 的研究指标：它属于软件系统的性能指标，在 12.7 节的系统指标与论文第 6 章的性能测试中单独评价，不与研究问题的结论混在一起报告。

### 4.3 研究假设（H1/H2/H3）

研究问题描述的是"要研究什么"，研究假设描述的是"用什么可检验的表述去研究"。三条假设改写为陈述式、可检验、不预设方向的表述：

- H1：在相同数据集、相同大语言模型与版本、相同 Embedding 模型、相同 Prompt 与相同上下文预算的条件下，KG-RAG（Vector RAG + 事件知识图谱）与 Vector RAG 在关系型、多跳型与时序型财经问题上的证据召回（Recall@K 与 Complete Evidence Recall@K）与 Answer Accuracy 存在差异。
- H2：在其他实验条件保持一致的情况下，不同系统检索深度（0-hop、1-hop、2-hop，即图谱扩展深度这一实验自变量）在证据覆盖与回答质量指标上存在差异。
- H3：在其他实验条件保持一致、且系统检索深度固定为 2-hop 的条件下，具有明确时间约束的问题子集上启用时间过滤与不启用时间过滤的方案在无关证据数量与证据检索质量上存在差异。

三条假设均为**陈述式、可检验、不预设方向的比较命题**，不预设知识图谱一定带来提升，也不预设提升的幅度；差异的方向与幅度由实验结果决定（本课题不引入额外的统计显著性检验，一律以统一的指标口径与判定规则给出结论）。RQ1 属于构建型问题，不设方向性假设。

说明：H1 中的证据召回由两个指标共同承担——Recall@K 衡量"召回了多少"，Complete Evidence Recall@K 衡量"是否被完整召回"；两者的定义与命名以 12.7 节为准。

三条假设与三个研究问题的对应关系是：H1 对应 RQ2，H2 与 H3 对应 RQ3。RQ1 的验证方式是第十二章 12.3 节的抽取评测集，用实体、事件、关系与时间四类抽取结果的 Precision、Recall 与 F1 说明结构化表示的质量。

研究问题、研究假设、实验、指标与结论形式之间的对应关系如下表。

| 研究问题 | 对应假设 | 对应实验 | 对应指标 | 结论形式 |
| --- | --- | --- | --- | --- |
| RQ2：知识图谱能否弥补传统向量 RAG 的关系检索不足 | H1 | 12.5 节的三组对比实验（Baseline 1／Baseline 2／Method；其中 **Baseline 2 ＝ 12.6 节 A 组，Method ＝ 12.6 节 C 组**，因此两者的对比等价于 A 与 C 的对比，唯一变量是"是否使用事件知识图谱"），其中 Baseline 2 与 Method 的对比为主要证据，并按 12.2 节的多标签子集分组统计 | Complete Evidence Recall@K、Answer Accuracy，辅以 Recall@K、Precision@K、MRR、Completeness、Faithfulness | 在关系型、多跳型与时序型子集上，KG-RAG 与普通 Vector RAG 的证据覆盖与回答正确性存在差异，并报告差异的方向与幅度 |
| RQ3：不同图谱检索策略对系统效果的影响（路径深度部分） | H2 | 12.6 节消融实验 A／B／C（Vector RAG／+1-hop KG／+2-hop KG） | Complete Evidence Recall@K、Answer Accuracy、Completeness | 在不同系统检索深度上，证据覆盖与回答质量指标存在差异，并报告差异的方向与幅度 |
| RQ3：不同图谱检索策略对系统效果的影响（时间约束部分） | H3 | 12.6 节消融实验 C 与 D 的对比（唯一变量为时间过滤），并在有时间约束的子集上单独统计 | 时序子集的 Complete Evidence Recall@K、Precision@K 与人工核查的无关证据条数 | 在具有明确时间约束的问题子集上，启用时间过滤与不启用时间过滤的方案在无关证据数量与证据检索质量上存在差异 |

这张对应表的作用是防止实验与结论脱节：每一条假设都能找到对应的实验设置与指标，每一个结论都能回溯到具体数据，而不是先写结论再补实验。无论最终结果是知识图谱带来增益、没有明显变化，还是只在部分子集上有效，只要实验按本文件的配置执行，都可以写成有效结论。

### 4.4 研究内容

围绕上述三个研究问题，本课题的研究内容包括四个方面：一是财经文本数据的采集、清洗与数据集构建，形成可复现的小规模数据集，并记录数据集版本级属性 dataset_version 与 data_cutoff_time；二是面向财经事件的知识图谱构建，包括实体识别、事件抽取、关系抽取、时间信息抽取、实体消歧、事件去重与图谱写入；三是向量检索与图谱检索相结合的混合检索方法实现，包括问题分析、实体识别、图谱扩展、时间过滤与证据排序；四是完整软件系统的设计与实现，以及功能测试、性能测试、检索实验、问答对比实验和消融实验。

## 五、项目边界

### 5.1 本系统不研究

- 股票涨跌预测
- 股票价格预测
- 自动交易
- 投资买卖建议
- 收益率预测
- 量化交易策略
- 完整证券投资决策

以上内容不作为系统功能，也不在论文中作出任何承诺。系统不提供买卖建议，不输出目标价或收益预测，不接入交易接口。此外，本系统不接入实时行情与实时推送，不处理图像、音频与视频等多模态与语音数据，第一版只处理文本；也不引入 Agent 自主规划与多智能体协作。这三项与预测类内容一样，不作为系统功能，也不在论文中作出任何承诺。如果答辩中被问及相关问题，回答口径统一为：本系统的定位是财经信息的组织、检索与问答，不涉及预测与投资决策。

### 5.2 本系统重点研究

- 财经信息检索
- 财经事件识别
- 实体关系关联
- 多跳关系查询
- 时序信息查询
- 证据检索
- 智能问答
- 知识图谱可视化

把"不研究"和"重点研究"两张清单同时写清楚，项目就从"大而空"变成"边界明确"：既便于在第 2 阶段撰写开题报告，也便于在第 3 阶段划分功能模块，更便于在第 6 章把结论限定在可控范围内。

### 5.3 公开事实与模型分析必须区分

系统输出必须明确区分"公开事实"与"模型分析"。图谱中的关系只表示公开信息中出现的关联，例如公司之间存在供应商关系、公司参与了某类事件，不能把图谱中的关系直接表述为因果关系，也不能由"A 公司与 B 公司存在供应链关系"直接推导出"B 公司一定会受到影响"。凡属推测性内容，必须在回答中显式标注为模型分析，并与有据可查的公开事实分开表述。这一条既是内容规范，也是系统在页面展示与提示词设计上必须落实的要求。

判断标准可以归纳为一句话：凡是能在文档中找到原文支撑的表述，属于公开事实；凡是需要模型进行推断、联想或猜测的表述，属于模型分析。系统在展示层用不同的区域与标签区分这两类内容，提示词中也要求模型在组织语言时不得把模型分析写成公开事实。

## 六、用户角色与功能需求

### 6.1 用户角色与登录范围决策

本科阶段只设置一个"普通用户"角色，不做多角色体系，不做细粒度权限控制，不做组织机构与审批流程，这样可以把开发精力集中在数据、图谱与检索方法上。普通用户角色的功能收敛为四项：提问与查看答案、图谱查看、来源与证据查看、历史记录查看。财经信息管理（文本数据的导入、删除、更新）不属于普通用户功能，列为后台系统能力，由作者通过脚本或受控后台入口执行数据导入、清洗、抽取、图谱更新与向量索引更新，不对普通用户开放。

关于注册与登录，本课题的执行决策是：第一版不启用注册与登录，用户直接进入系统即可使用提问与查看答案、图谱查看、来源与证据查看与历史记录功能。系统面向的是公开的财经文本信息，不涉及个人隐私数据与敏感业务数据，在本科毕业设计的演示与实验场景下，登录环节只会增加联调工作量，而对论文的核心问题（RQ1、RQ2、RQ3）没有贡献，因此第一版把用户管理从必需功能中移出。需要说明的是，不启用登录并不等于匿名用户之间的历史记录互相可见：前端在首次访问时生成 session_id = UUID 并存于浏览器，question 表记录该 session_id，历史记录按 session_id 过滤，从而把不同匿名会话的问答历史隔离开。

与这一决策配套的数据设计是：user 表在数据模型中**固定保留并始终建立**，字段与用途在本文件 9.1 节中给出；第一版不启用登录业务，该表保持为空、不写入数据，以保证数据库 schema 稳定、表数量恒为六张，后续启用登录时不需要改动表结构。question 与 answer 表仍然保留 user_id 字段，登录未启用时该字段可以为空；question 表同时记录 session_id 字段（浏览器生成的 UUID），用于按会话隔离历史记录。表数量恒为六张，不新增表，也不因"不启用登录"而少建表。

这一决策是条件性决策，不是永久删除用户功能。若学校论文模板或毕业设计要求明确体现用户管理功能，则最小化开启"仅登录"：只提供登录入口与会话管理，不做注册、不做找回密码、不做多角色与细粒度权限。也就是说，一旦模板有要求，只需要在现有 user 表与后端接口上做最小改动即可满足，而不会影响本文件第六至第十二章的其他内容。在论文写作时，用户角色分析一节按实际情况表述：需求分析阶段给出普通用户角色与可选登录的设计考虑，系统实现与测试章节按最终是否启用登录如实说明，不虚构未实现的功能。

### 6.2 功能需求（六个模块）

功能 1：财经信息管理（后台系统能力，不对普通用户开放）。由作者通过脚本或受控后台入口实现财经新闻、公告、政策等文本数据的导入、分类、清洗、查询、删除与更新。导入时记录标题、正文、发布时间、来源、URL、涉及公司与入库时间（ingest_time）等字段，保证后续向量化与事件抽取有统一的数据入口。文档新增、修改或删除后，触发一次重新处理流程，使图谱与向量索引与文本内容保持一致。

功能 2：财经事件抽取。从文本中识别公司、人物、行业、机构、事件、政策六类实体以及时间、关系，并将抽取结果写入知识图谱。抽取结果需要保留证据来源，便于在问答环节回溯到原文；除 EVIDENCED_BY 外，其余 8 条核心语义关系都需要记录来源文档编号、来源文本块编号与置信度（EVIDENCED_BY 本身即事件到文档的证据关系，不重复携带），事件同样需要记录来源文档编号与置信度；抽取完成后进入实体消歧与事件去重环节。

功能 3：事件知识图谱。支持实体查询、事件查询、关系查询、时间查询、多跳路径查询与图谱可视化。用户既可以直接检索某个实体或某类事件，也可以查看实体之间的路径。

功能 4：智能问答。用户输入自然语言问题，例如"A 公司最近发生了哪些重大事件？"，系统依次完成问题分析、文本检索、图谱检索、时间过滤、证据排序与大模型生成，返回自然语言回答。问题中的"最近"按 event_time 判定（事件发生时间落在数据截止时间前 30 天以内），并以数据截止时间为基准解释。

功能 5：证据追溯。答案下方展示回答来源、新闻来源、公告来源、相关事件，以及使用了图谱扩展时的图谱路径；若本次回答未使用图谱扩展，则显式标注"本次回答未使用图谱扩展"。用户可以从答案直接定位到具体文档、文本块与图谱关系，这是本系统的重点展示功能。

功能 6：历史问答。保存用户问题、提问时间、回答与引用证据，支持后续查看，同时为实验统计提供数据来源。第一版不启用登录时，历史记录按浏览器生成的 session_id 过滤，不同匿名会话之间不互相可见；启用登录后可把会话关联到 user_id。历史记录不需要独立的表：question 记录问题，answer 记录回答，answer_evidence 记录答案引用的证据，三者通过 question_id 与 answer_id 关联即可还原完整的问答历史。

### 6.3 用例概述

系统的用例结构如下：

```text
用户
 ├── 提交问题
 ├── 查看答案
 ├── 查看来源
 ├── 查看图谱
 └── 查看历史记录
```

普通用户提交问题后，系统返回答案、证据来源，以及使用了图谱扩展时的图谱路径；若本次回答未使用图谱扩展，则标注"本次回答未使用图谱扩展"。用户可以进一步查看原始文档与知识图谱可视化结果，也可以在历史记录中回看此前的问答。第一版不启用登录时，上述用例全部对匿名用户开放，但历史记录按浏览器生成的 session_id 过滤，不同匿名会话之间不互相可见。财经信息管理（导入、删除、更新）不在上述用例中，属于后台系统能力，不对普通用户开放；若按 6.1 节的条件下开启"仅登录"，则在提交问题与查看历史记录之前增加一个登录步骤，其余用例不变。

## 七、非功能需求

| 需求项 | 要求说明 | 验收口径（可量化或可验证） |
| --- | --- | --- |
| 响应时间 | 问答请求需要在可接受的时间内返回，检索与生成的耗时要能分段统计，便于定位性能瓶颈 | 在统一的测试环境下记录平均回答耗时与 P95 耗时，并分别记录向量检索、图谱查询与大模型生成的耗时；该口径在第 12 节系统指标与论文第 6 章性能测试中保持一致 |
| 并发能力 | 系统需支持小规模并发访问，不因并发请求出现崩溃或数据错乱 | 10 个用户并发发起问答请求，系统不崩溃、不出现数据错乱，接口成功率不低于 95% |
| 可维护性 | 采用前后端分离与模块化设计，模块边界清晰；模型接口地址、数据库连接、检索参数等集中在配置文件中管理 | 更换大模型 API 或 Embedding 模型时不需要修改前端代码；按部署说明可在新环境中完成一次完整部署 |
| 数据一致性 | 不做实时同步。文档新增、修改或删除后，由系统触发重新处理并更新 Neo4j，必要时重建或增量更新向量索引；提供一致性检查手段 | 文档变更后通过一次重新处理流程完成图谱与向量索引更新，不残留失效数据；提供一致性检查手段，可核对抽取结果与图谱节点数量、文本块数与向量条数 |
| 异常处理 | 对模型接口超时、图谱查询为空、输入为空、文件格式错误等异常给出明确提示 | 构造不少于 6 类异常场景逐一测试，系统不出现未捕获异常导致的页面失效或服务中断，错误提示不泄露密钥与内部堆栈信息 |
| 系统安全性 | API 密钥不写入前端代码，接口做输入校验与请求频率限制；口令加密存储的要求仅在启用登录功能的条件下适用 | 密钥仅保存在后端环境变量或配置文件中，问答接口具备输入长度与请求频率的基本限制；若启用登录，则口令以摘要形式存储，不保存明文口令 |

上述验收口径中的**响应时间绝对值**目标在系统首次实测后固化，其余各项的口径本身（成功率百分比、异常场景数量、二元判定条件）即为其验收标准，无需再固化；所有口径均保证在第 12 节系统指标与论文第 6 章性能测试中使用同一口径，避免出现前后不一致的表述。异常处理一行中的"文件格式错误"场景，其允许的文件格式清单与必填字段清单在实现阶段固定，并与论文第 6 章 6.1 测试环境与实验配置一同记录；本文件不预先限定录入格式，以保证该异常场景可以构造、可以判定。数据一致性一行的口径已经调整为"变更后重新处理 + 一致性检查"，不再承诺 MySQL、Neo4j 与向量索引之间的实时同步：这三者承担不同职责，实时同步既超出本科阶段可实现与可验证的范围，也会引入与课题核心问题无关的工程复杂度。可验证的做法是提供一次完整的重新处理脚本与一组一致性检查语句，在文档变更后运行并记录结果。

## 八、系统总体架构

### 8.1 六层架构图

```text
┌─────────────────────────────┐
│         用户交互层           │
│    Web前端 / 智能问答界面    │
└──────────────┬──────────────┘
               ↓
┌─────────────────────────────┐
│         业务服务层           │
│     用户管理 / 问答管理      │
│     查询管理 / 历史记录      │
└──────────────┬──────────────┘
               ↓
┌─────────────────────────────┐
│         智能问答层           │
│   问题分析 → 检索 → 证据融合 │
│   → Prompt构建 → LLM生成     │
└───────┬─────────────┬───────┘
        ↓             ↓
┌──────────────┐ ┌────────────────┐
│  向量检索模块 │ │  图谱检索模块   │
│   FAISS等     │ │     Neo4j       │
└──────┬───────┘ └───────┬────────┘
       ↓                  ↓
┌──────────────────────────────────┐
│          数据知识层               │
│ 新闻 / 公告 / 政策 / 财经信息     │
│ 事件/公司/行业/机构/人物/政策     │
└──────────────────────────────────┘
```

该图保持 v1.0 的六层结构：向量检索模块以 FAISS 为代表实现，图中保留"向量检索模块 FAISS等"与"图谱检索模块 Neo4j"的原始标注，层级结构不变；数据知识层一行已按 第9.2.1节 的 6 类实体补齐为公司、行业、机构、人物、政策与事件，时间不以实体形式列出（时间以 Event 节点的 event_time、关系属性 valid_from／valid_to 与数据集版本级属性 data_cutoff_time 表达，见 第10.1节）。需要说明的是，图中"向量检索模块"在实现上对应第 9.3 节的向量索引，FAISS 是向量相似度检索库，不按"向量数据库"表述。

### 8.2 各层职责

用户交互层负责与用户直接交互，提供问答输入、答案展示、来源与证据查看、图谱可视化与历史记录等页面。该层只负责界面与交互逻辑，不直接访问数据库与模型。

业务服务层负责用户管理、问答管理、查询管理与历史记录，接收前端请求、校验参数、组织业务流程并返回结果。用户的每一次提问、回答与引用证据都由该层落库；第一版不启用登录时，用户管理模块保留最小形态，问答管理、查询管理与历史记录正常提供。

智能问答层是系统的核心处理单元，依次完成问题分析、检索、证据融合、Prompt 构建与大模型生成。问题分析负责判定问题类型与是否需要图谱扩展，证据融合负责把向量检索结果与图谱检索结果合并排序。

向量检索模块基于 FAISS 完成文本语义检索，输入是问题的向量表示，输出是候选文本块及其对应文档编号，用于提供原始文本证据。

图谱检索模块基于 Neo4j 完成实体、事件与关系的多跳查询，输出实体路径、事件属性与关联关系，用于补充文本检索难以覆盖的关系信息与时间信息。

数据知识层统一管理新闻、公告、政策等原始财经信息，以及从文本中抽取的事件、公司、行业、机构、人物与政策六类实体等结构化知识；时间信息不作为独立实体，而是以 Event 节点的 event_time、关系属性 valid_from／valid_to 与数据集版本级属性 data_cutoff_time 表达（见 第10.1节）。该层是向量检索与图谱检索共同的数据来源。

### 8.3 技术栈

| 模块 | 技术选型 | 用途 | 选型理由 |
| --- | --- | --- | --- |
| 前端 | Vue 3 | 实现智能问答界面、知识图谱可视化、来源与证据查看与历史记录页面 | 组件化开发，上手成本低，适合单人完成界面部分 |
| 后端 | Python + FastAPI | 提供问答、数据、图谱、历史记录等接口，串联检索与生成流程 | Python 与数据处理、AI 生态一致，FastAPI 轻量且接口定义清晰、便于调试与测试 |
| 关系数据库 | MySQL | 存储用户（可选）、文档元数据、文本块、问题、回答与证据关联 | 关系模型适合结构化业务数据，增删改查与事务处理成熟 |
| 图数据库 | Neo4j | 存储实体、事件与关系，支持多跳路径查询 | 原生图存储与 Cypher 查询语言适合表达多跳关系与路径检索 |
| 向量索引 | FAISS | 保存文本块向量并完成相似度检索 | FAISS 本质是向量相似度检索库（索引库），轻量、可本地运行、不依赖额外服务，适合本科阶段的数据规模 |
| 模型 | Embedding 模型 + 大语言模型 API | Embedding 模型负责文本与问题的向量化，大语言模型 API 负责基于证据生成答案 | 不本地训练与部署大模型，把精力集中在系统设计与检索方法上；**Embedding 模型与版本已在第 5 阶段固化，大语言模型仍 TBD（选型后固化）**，两者与调用方式一并记录在 第12.4节 的实验配置固定表中 |
| 数据处理 | Python | 数据清洗、文本切分、实体识别、事件抽取、实体消歧、事件去重与图谱写入 | 与后端语言一致，脚本可复用，便于长期维护 |
| 部署 | Docker | 统一运行环境，打包后端与依赖服务 | 降低环境差异带来的部署问题，便于答辩现场快速启动 |

表中"向量索引"一行是 v2.0 的术语口径：技术选型仍是 FAISS，但其定位是向量索引与向量检索组件，不再写成"向量数据库"。模型一行中的 Embedding 模型与大语言模型的具体名称、版本与调用方式在第 12.4 节的实验配置固定表中记录。口径是**选型完成后才写入型号**：第 5 阶段已把 Embedding 模型与 revision 固化，故 第12.4节 与本行据此写明；大语言模型尚未选型，仍填 TBD，不提前写入未经确认的型号。

### 8.4 明确不引入的技术

本课题不同时使用 LangChain + LlamaIndex，不引入多个向量数据库，也不引入多个图数据库。技术越多不代表毕业设计越高级：每增加一个组件，都会带来额外的学习成本、调试成本和故障点，而这些组件解决的问题并不是本课题要验证的问题。本课题要证明的是知识图谱在财经问答检索中的作用，因此检索、图谱扩展、时间过滤与证据排序流程由后端代码直接实现，一方面便于在论文中逐步说明每个环节做了什么，另一方面便于在消融实验中精确控制变量。

具体来说，以下技术明确不引入本课题：不引入 LangChain 与 LlamaIndex，避免框架把检索与拼装流程封装成黑盒，导致消融实验无法逐项控制变量；不引入 Milvus、Qdrant、Weaviate 等独立向量数据库系统，因为 FAISS 已经能够满足本课题的数据规模与检索需求，独立向量数据库会带来额外的服务部署与运维工作；不引入 Elasticsearch 等独立检索引擎，检索策略全部由后端代码实现；不引入 Kafka、微服务架构与 Kubernetes，系统按单体后端加前端分离的方式部署在一台机器上，已经能够满足 10 个并发用户的小规模访问要求。此外，本课题不引入股票预测、投资建议、自动交易、股吧数据源与多角色权限体系；也不引入 Agent 框架与多智能体编排，不引入多模态与语音能力（第一版只处理文本），不接入实时行情与实时推送（系统基于离线数据集运行，以数据截止时间标注数据版本）。这些属于项目边界之外的内容，已在第五、六、八章中说明。

## 九、数据设计概要

### 9.1 MySQL 核心表（六张）

MySQL 收敛为六张表，分别是 user、document、document_chunk、question、answer、answer_evidence。与 v1.0 相比，新增了 document_chunk 与 answer_evidence，删除了 history 表，question 与 answer 的职责保持稳定；v2.1 新增的 session_id 是 question 表的字段而不是新表，数据截止时间 data_cutoff_time 是数据集版本级属性、不属于任何表，因此表数量仍为六张。六张表**全部建立**：user 表在第一版不启用登录业务时保持为空、不写入数据，不采用"按条件决定是否建表"的做法，这样表数量、外键关系与 schema 在任何情况下都一致，论文口径与答辩口径统一为"六张表"。

| 表名 | 用途 | 关键字段 |
| --- | --- | --- |
| user | **始终建立**。仅在启用登录功能时使用，存储账号与登录信息；第一版不启用注册与登录，该表保持为空、不写入数据，以保证表数量恒为六张、schema 稳定 | user_id（主键）、username、password_hash、create_time |
| document | 存储导入的财经文本及其元数据，是向量化与事件抽取的数据来源 | doc_id（主键）、title、content、source、url、publish_time、ingest_time（入库时间）、category、company_list、create_time |
| document_chunk | 存储文档切分后的文本块，是向量检索与证据定位之间的中间层 | chunk_id（主键）、doc_id（外键）、chunk_index、content、token_count、vector_id |
| question | 存储用户提交的问题；task_type、gold_hop_depth、time_constraint 为测试集标注字段，非测试集题目留空 | question_id（主键）、user_id（外键，登录未启用时可为空）、session_id（浏览器生成的 UUID）、question_text、task_type、gold_hop_depth、time_constraint、ask_time |
| answer | 存储系统生成的回答及其生成信息 | answer_id（主键）、question_id（外键）、answer_text、graph_path（第一版以 JSON 文本存储，后续可归一化为独立表）、is_graph_extended（本次回答是否使用了图谱扩展，0／1；决定证据区域显示路径还是显式标注）、model_name、prompt_version、create_time |
| answer_evidence | 存储答案与证据文档、证据文本块之间的关联，是证据追溯的核心表 | answer_id（外键）、doc_id（外键）、chunk_id（外键）、rank、evidence_type |

各表的职责可以进一步理解为：document 保存原始文本与来源信息，是文本内容的权威来源，其中 publish_time 表示文档发布时间、ingest_time 表示该文档的入库时间，数据集版本级属性 data_cutoff_time 不存放在该表中；document_chunk 保存切分结果，使检索命中的粒度从整篇文档缩小到文本块；question 保存问题本身以及测试集标注信息，task_type、gold_hop_depth、time_constraint 三个字段为第 12.2 节的多标签测试集服务，普通用户提问时这三项留空，其中 gold_hop_depth（标注路径深度）是问题的金标准属性，记录该问题在客观上最少需要沿几条图谱关系才能完整得到答案，与 12.6 节消融实验中的检索深度不是同一概念；session_id 由前端首次访问时生成并存于浏览器，用于隔离不同匿名会话的历史记录；answer 保存回答正文、图谱路径与生成信息，其中 model_name 与 prompt_version 用于实验期间固定模型与 Prompt 版本；answer_evidence 保存答案引用了哪些文档与哪些文本块，以及它们的排序与证据类型，用于回答页面的证据列表与实验中的证据召回统计。

历史记录不需要独立的 history 表。用户查看历史问答时，系统按 question 与 answer 的关联关系查询：以 question_id 连接 question 与 answer，即可得到问题、提问时间、回答与生成信息；需要展示证据时再通过 answer_id 关联 answer_evidence，并进一步连接 document 与 document_chunk 得到文档标题、来源、发布时间与原文链接。第一版不启用登录，查询历史记录时按当前浏览器的 session_id 过滤，因此不同匿名会话之间不会互相看到彼此的问答历史，user_id 在该场景下为空。这样设计既避免了 history 表与 question、answer 表之间的信息重复，也避免了三张表之间出现不一致的可能，同时不需要新增会话表。

向量检索与原文之间的映射链路是证据追溯的数据基础，链路如下：

```text
Document（原始文章）
    ↓ 切分
Chunk（文本块，记录 chunk_id 与 doc_id）
    ↓ Embedding
Embedding（向量）
    ↓ 写入
向量索引（FAISS，向量附带 vector_id）
```

检索时的反向定位链路如下：

```text
用户问题向量
    ↓ 相似度检索
返回 vector_id
    ↓
chunk_id
    ↓
doc_id
    ↓
原始文章（标题、来源、URL、publish_time）
```

也就是说，向量索引中不保存权威文本，只保存向量与索引结构；向量与原文的对应关系由 vector_id、chunk_id、doc_id 三级编号串联。回答中的每一条文本证据都可以沿着这条链路定位到原始文章，这正是 9.1 节保留 document_chunk 表、并把 vector_id 写入该表的原因。

表中字段可以根据实现情况微调，但表的职责与数据来源保持一致。若在开发过程中新增字段，需要在论文的数据库设计章节同步说明，避免文档与实现不一致。

### 9.2 Neo4j 本体设计

#### 9.2.1 实体类型（6 类）

实体类型由 v1.0 的 8 类收敛为 6 类，标签统一使用英文命名，便于在 Neo4j 与 Cypher 中直接使用。

| 实体类型（标签） | 中文名称 | 说明 |
| --- | --- | --- |
| Company | 公司 | 上市公司及在财经信息中出现的其他企业主体，是图谱的核心节点，以 stock_code 作为稳定业务唯一标识 |
| Person | 人物 | 公司高管、监管机构人员等与事件相关的人物 |
| Industry | 行业 | 公司所属行业以及事件涉及的行业 |
| Institution | 机构 | 监管机构、交易所、中介机构等 |
| Event | 事件 | 财经事件，本课题的核心知识单元 |
| Policy | 政策 | 与公司或行业相关的政策文件及其主题 |

Product（产品）与 Location（地点）列为可扩展类型，不进入第一版的本体、抽取规则与评测范围。收敛的理由是工作量控制：每增加一类实体，都需要配套的抽取规则、消歧策略、数据来源与评测工作量，产品名称的边界尤其模糊（产品型号、服务名称、业务板块之间难以稳定区分），地点在财经事件中也往往不是回答问题的必要信息。把这些类型登记为可扩展项，可以在论文的"总结与展望"中说明后续扩展方向，而不占用第一版的开发与评测预算。

#### 9.2.2 核心事件类型（8 种）

- 业绩事件
- 监管事件
- 股权事件
- 投资并购事件
- 重大合同事件
- 产品事件
- 政策事件
- 重大经营事件

第一版固定为以上 8 种核心事件类型，作为抽取、标注与评测唯一的 schema，不再保留浮动的事件类型数量表述。理由：人工标注、抽取 Precision／Recall／F1 与分组统计都需要固定 schema，浮动范围会使标注与评测口径不可复现。事件类型与实体类型不同：实体类型收敛为 6 类，事件类型固定为 8 种，原因是财经问答中的问题大多围绕"发生了什么"展开，事件类型的细分直接决定问题分类与检索策略是否能够命中，因此这一维度保留必要的细度。

#### 9.2.3 核心关系清单

核心关系由 v1.0 的 9 条中文关系改写为 9 条英文命名的关系，并完成去重，最终清单如下：

```text
(Company)-[:BELONGS_TO {valid_from, valid_to, source_doc_id, source_chunk_id, confidence}]->(Industry)
(Company)-[:SUPPLIES {source_doc_id, source_chunk_id, confidence}]->(Company)
(Company)-[:CUSTOMER_OF {source_doc_id, source_chunk_id, confidence}]->(Company)
(Company)-[:COMPETES_WITH {source_doc_id, source_chunk_id, confidence}]->(Company)
(Company)-[:HAS_EXECUTIVE {source_doc_id, source_chunk_id, confidence}]->(Person)
(Company | Person | Institution)-[:PARTICIPATES_IN {role, source_doc_id, source_chunk_id, confidence}]->(Event)   // role ∈ {主体, 合作方, 涉及方, 监管方, 受影响方}
(Event)-[:ISSUED_BY {source_doc_id, source_chunk_id, confidence}]->(Institution)                                 // 仅政策事件与监管事件
(Event)-[:RELATED_TO {source_doc_id, source_chunk_id, confidence}]->(Policy)
(Event)-[:EVIDENCED_BY]->(Document)                                                                              // 事件与其证据文档；本身即证据关系，不重复携带 source_doc_id
```

各条关系的含义与用途如下表。

| 关系 | 方向 | 属性 | 说明 |
| --- | --- | --- | --- |
| BELONGS_TO | Company → Industry | valid_from、valid_to、source_doc_id、source_chunk_id、confidence | 公司所属行业，带有效时间区间，用于判断关系在查询时间点是否成立；若行业归属来自外部行业分类而非文本抽取，该分类文件同样作为一份 Document 入库，source_doc_id 指向它，保证证据链不断 |
| SUPPLIES | Company → Company | source_doc_id、source_chunk_id、confidence | 供应商关系，A 公司向 B 公司供货；方向按信息来源标注，不做双向冗余存储 |
| CUSTOMER_OF | Company → Company | source_doc_id、source_chunk_id、confidence | 客户关系，与供应商关系互为反向，本课题按抽取结果确定方向，避免同一条关系存两份 |
| COMPETES_WITH | Company → Company | source_doc_id、source_chunk_id、confidence | 竞争关系，通常为对称关系，第一版按抽取方向存一条 |
| HAS_EXECUTIVE | Company → Person | source_doc_id、source_chunk_id、confidence | 公司高管任职关系，必须带齐证据三元组，人物与公司之间的职务信息记录在文档与事件中 |
| PARTICIPATES_IN | Company／Person／Institution → Event | role、source_doc_id、source_chunk_id、confidence | 公司、人物或机构参与事件，role ∈ {主体, 合作方, 涉及方, 监管方, 受影响方}；同一机构为发布／作出方时只写 ISSUED_BY，不重复写本关系 |
| ISSUED_BY | Event → Institution | source_doc_id、source_chunk_id、confidence | 政策事件与监管事件中政策的发布机构或监管处罚的作出机构，表示该机构是发布／作出方；其他事件类型不产生该关系 |
| RELATED_TO | Event → Policy | source_doc_id、source_chunk_id、confidence | 事件与相关政策之间的关联，必须带齐证据三元组 |
| EVIDENCED_BY | Event → Document | 无（本身即事件到文档的证据关系，不再重复携带 source_doc_id） | 事件与其证据文档之间的关联，一个事件可以对应多份文档 |

v1.0 中的 (Event)-[:INVOLVES]->(Company) 已删除。删除的理由是：它与 PARTICIPATES_IN 表达同一信息，只是在方向上相反；同时保留两条方向相反的关系意味着同一条事实需要在图谱中写入两份，任何一次修正都必须同时修改两个位置，容易出现更新不一致，而且会让路径计数、事件参与主体统计与去重逻辑出现重复计算。反向查询并不需要额外的关系：需要查找"某个事件涉及哪些主体"时，用 Cypher 从 Event 节点反向遍历 PARTICIPATES_IN 即可得到，例如 MATCH (e:Event {event_id: $event_id})<-[:PARTICIPATES_IN]-(p) RETURN p。事件参与主体不再限定为公司：PARTICIPATES_IN 的定义域为 Company | Person | Institution → Event，role ∈ {主体, 合作方, 涉及方, 监管方, 受影响方}；participants 不再作为事件属性存储，事件参与主体统一由 PARTICIPATES_IN 关系与 ISSUED_BY 关系表达，角色信息写在关系属性 role 中；这样参与主体既不会在属性与关系之间重复，也可以在查询时按角色过滤。为避免同一条事实写两份，去重规则为：同一机构既是政策或监管事件的发布／作出方又参与该事件时，只写 ISSUED_BY，不再重复写 PARTICIPATES_IN；"监管方"这一 role 仅用于"参与事件但不是发布／作出方"的机构。

所有抽取事实统一保留证据，这是图谱从"关系图"变为"有证据的关系图"的关键。除 EVIDENCED_BY 外，其余 8 条核心语义关系统一携带 source_doc_id、source_chunk_id 与 confidence 三项证据属性，三项证据属性是必需属性而不是可选补充；EVIDENCED_BY 本身承担事件与证据文档之间的关联作用，因此不再重复携带 source_doc_id。其中 BELONGS_TO 另带 valid_from、valid_to，若行业归属来自外部行业分类而非文本抽取，该分类文件同样作为一份 Document 入库，source_doc_id 指向它，保证证据链不断；HAS_EXECUTIVE、ISSUED_BY、RELATED_TO 同样必须带齐三项证据属性。这样处理之后，系统对任意一条事实都能回答"来自哪篇文档、哪个文本块、置信度多少"，而不是只给出一个无法核查的结论，与第十三章"答案＋文本证据＋图谱路径"的闭环一致。

一个事件可以对应多份文档，公告、多篇报道与政策文件都通过 EVIDENCED_BY 挂在同一个事件节点上，示意如下。

```text
                     ┌──────────────────────────┐
                     │ Event：监管处罚事件       │
                     │ event_time = XXXX-XX-XX  │
                     └────────────┬─────────────┘
                                  │ EVIDENCED_BY
        ┌─────────────────────────┼─────────────────────────┐
        ↓                         ↓                         ↓
┌───────────────┐        ┌───────────────┐        ┌───────────────┐
│ Document：公告 │        │ Document：报道 │        │ Document：报道 │
│ publish_time   │        │ publish_time   │        │ publish_time   │
│ = XXXX-XX-XX   │        │ = XXXX-XX-XX   │        │ = XXXX-XX-XX   │
└───────────────┘        └───────────────┘        └───────────────┘
```

该结构同时完成了时间归一化：事件发生时间存放在 Event 节点，公告、媒体报道与政策文件的发布时间统一存放在各自 Document 节点的 publish_time 上，来源类型由 Document.source 与 category 区分，不再单独定义"信息发布时间"；事件不再被压缩到一个文档上，也不再只保留一个时间字段。同一事件的多篇文档可以按发布时间排序，用户看到的是"事件发生时间 + 各来源的发布时间"这一组完整的时间信息。

#### 9.2.4 事件属性

事件不能只做一个节点。如果图谱中只写成"A 公司 → 监管处罚"，那么系统无法回答事件发生的时间、涉及哪些主体、由哪个机构发布、依据什么来源，也无法进行时间约束检索，更无法追溯到原始公告，所谓"事件驱动"就只剩一个空标签。因此事件必须作为带属性的知识单元存在。v2.0 对事件属性做了瘦身，核心属性只有以下六项。

| 属性 | 含义 | 监管处罚事件示例 |
| --- | --- | --- |
| event_id | 事件的唯一标识 | EVT-0001 |
| event_type | 事件类型，取值来自核心事件类型 | 监管事件 |
| event_name | 事件名称 | 某公司因信息披露违规被出具警示函 |
| event_time | 事件发生时间 | XXXX-XX-XX |
| description | 事件描述 | 一句话说明事件经过 |
| confidence | 抽取置信度 | 0.0～1.0 之间的数值 |

除以上六项核心属性外，trigger（触发词）作为可选辅助字段保留，仅用于抽取调试与错误分析，不作为查询依据，也不进入检索与实验的判定逻辑。participants 与 publish_time 已从事件属性中移除：participants 由 PARTICIPATES_IN 关系（含 role 属性）与 ISSUED_BY 关系共同表达，事件与参与主体的对应关系不再以数组形式存放；publish_time 与 source、url 一起归属 document，事件与文档之间用 EVIDENCED_BY 关系表达，从而支持一个事件挂多份文档。这样处理之后，事件节点的属性数量可控，属性与关系各司其职，同一信息不会在图谱中出现两个互相矛盾的副本。

以"监管处罚事件"为例，事件节点的展开形式如下：

```text
监管处罚事件
│
├── 事件主体：A公司（PARTICIPATES_IN，role = 主体）
├── 处罚机构：某监管机构（ISSUED_BY，本事件属监管事件，该机构为处罚作出方，不再重复写 PARTICIPATES_IN）
├── 事件时间：XXXX-XX-XX
├── 事件类型：监管处罚
├── 事件描述：……
└── 证据文档：公告（EVIDENCED_BY → document.doc_id）
```

补充说明：事件中的具体日期在最终数据集确认后填写，本文件不提前填入未经核实的数字。confidence 字段用于标记抽取结果的可靠程度，在问答与实验分析中可以据此过滤低置信度事件。

#### 9.2.5 实体消歧与事件去重

实体消歧解决"同一个实体被抽成多个节点"与"不同实体被合并成一个节点"两类问题。财经文本中，同一家公司在不同来源中可能以全称、简称、别名或历史名称出现，例如公司更名前后两个名称同时出现在数据集中；反过来，不同公司也可能因为简称相似而被误判为同一家企业。本课题的实体对齐以 stock_code 为锚：Company 节点以 stock_code 作为稳定业务唯一标识，属性包括 stock_code、company_name、short_name、aliases（数组，含历史名称）与 exchange。抽到的公司名称先与别名表匹配，匹配成功则归并到对应的 stock_code 节点；匹配失败进入待消歧列表，人工确认后再写入图谱。

公司唯一标识之所以必须用股票代码，是因为 A 股公司存在更名、简称变化、同名与全称／简称不一致的情况：名称会随时间变化，而股票代码在数据集覆盖的时间范围内保持稳定。图谱内部以股票代码定位公司，名称与别名作为属性用于展示与匹配，这直接影响实体对齐、去重、图谱准确性与检索准确性。若直接用公司名称当主键，一旦公司更名，图谱中旧名称节点与新名称节点会同时存在，供应商、客户、事件参与主体等关系会被拆到两个节点上，多跳查询与事件统计都会出现遗漏或重复。

事件去重解决"同一个事件被抽成多个事件节点"的问题。同一件事通常同时出现在公告与多篇报道中，如果不去重，图谱中会出现多个描述相同、时间相近、参与主体相同的事件节点，导致事件数量统计虚高、检索结果重复、多跳路径出现多条等价路径，最终影响实验结论。本课题的事件去重规则是：按 event_type + 参与主体 + 时间窗口 + 触发词相似度判定同一事件的多来源描述，四项条件同时满足时判定为同一事件并合并；合并后保留全部证据文档，通过 EVIDENCED_BY 关系全部挂在合并后的事件节点上，发布时间最早的一份作为主要来源展示，其余来源作为补充证据保留。合并动作记录在抽取日志中，便于失败案例分析时核查。

实体消歧与事件去重是财经知识图谱构建中必须单独处理的一环，不能寄希望于后续查询阶段自动解决。这一环节的工作量计入第 6 阶段"事件抽取与知识图谱"的工期，评价方式与 12.3 节的抽取评测集共用同一套人工标注数据：Dev 集（50～100 条，建议 60 条）用于迭代核查，Test 集（200 条）在规则冻结后一次性评测；实体层面检查是否合并正确，事件层面检查是否多抽或漏并。

### 9.3 向量索引

FAISS 中只存放 document embedding。文档按固定长度切分为文本块后，由 Embedding 模型编码为向量写入索引，每个向量附带文档编号与文本块编号，用于建立向量与原文的对应关系。检索时，用户问题经过同一个 Embedding 模型编码，在索引中做相似度搜索，返回若干候选文本块。

需要纠正一个术语：FAISS 不是向量数据库，而是向量相似度检索库（索引库）。它只提供向量索引构建与相似度检索能力，不提供独立服务进程、分布式部署、数据持久化与事务管理等数据库特性，与 Milvus、Qdrant、Weaviate 这类独立向量数据库系统在定位上不同。因此本文件统一把这一部分表述为"向量索引"或"向量检索组件"，第 8.3 节技术栈表中该行的模块名称为"向量索引"。

三个存储的分工需要明确：MySQL 负责业务数据与文档元数据，是文本内容的唯一权威来源；Neo4j 负责实体、事件与关系，支撑多跳路径与时间查询；向量索引只负责文本语义相似度检索，不保存权威文本，只保存向量与索引结构。三者通过文档编号、文本块编号、事件编号与实体标识建立对应关系：向量检索回答"哪些文本与问题语义相近"，图谱检索回答"这些实体之间有什么关系"，两者结合才能同时提供证据与关系。

索引的更新策略与第 7 章的数据一致性口径一致：文档新增、修改或删除后，由系统触发重新处理，重新切分并计算向量；小规模变更按增量方式写入，文档删除或切分规则调整时整体重建索引。每次重建后运行一致性检查，核对 document_chunk 的文本块数量与向量索引中的向量条数是否一致，并核对抽取结果数量与图谱节点数量是否落在预期范围内。

## 十、时间维度设计

### 10.1 四种时间及其归属层级

财经信息的时间信息往往不止一个。本课题至少区分以下四种时间，并明确每种时间存放在哪一层，避免把不同含义的时间混在同一个字段上。

| 时间类型 | 归属层级 | 含义 | 示例 | 主要用途 |
| --- | --- | --- | --- | --- |
| 事件发生时间 | Event 节点的 event_time | 事件实际发生的时间 | 2025-03-01 | 按事件时间做时间范围过滤，并作为"最新／最近／近期"的判定依据 |
| 文档发布时间 | Document 节点的 publish_time | 公告、媒体报道或政策文件的发布时间 | 2025-03-05 | 判断信息何时进入公开渠道，并区分原始信息与二次报道 |
| 关系有效时间 | 关系属性 valid_from、valid_to | 关系成立与失效的时间区间 | 某年度内的行业归属 | 判断关系在查询时间点是否有效 |
| 数据截止时间 | 数据集版本级属性 data_cutoff_time | 数据集版本人为确定的覆盖截止边界 | 2026-09-25（v1.0 实际取值） | 作为"最新／最近／近期"等相对时间表述的基准 |

四种时间的归属层级是 v2.0 的重点：事件发生时间存放在 Event 节点；公告、媒体报道与政策文件的发布时间统一存放在 Document 节点，由 publish_time 表示，来源类型由 Document.source 与 category 区分，不再单独定义"信息发布时间"；关系有效时间以关系属性 valid_from 与 valid_to 表达，目前用于 BELONGS_TO 这类随时间变化的关系；数据截止时间是数据集版本级属性，与 dataset_version 成对记录在数据集元信息中，不属于任何表，详见 10.3 节。时间信息按层级分开存放之后，同一份文档可以支撑多个事件，同一个事件也可以挂多份文档，事件时间不再被压成单值，文档发布时间也不会被误当作事件发生时间。

### 10.2 时间线示例与时间约束检索

一条典型的时间线如下：

```text
2025-03-01
事件发生

2025-03-05
公司发布公告

2025-03-06
媒体报道
```

当用户提出"2025年3月以后发生了哪些相关事件？"时，系统需要按照以下步骤处理：第一步，问题分析识别出时间约束（2025 年 3 月以后）与查询目标（相关事件），必要时识别问题中的实体；第二步，用问题与实体关键词进行文本检索，得到候选文档；第三步，进入知识图谱，按事件发生时间（event_time）过滤事件节点，并沿 PARTICIPATES_IN、RELATED_TO 等关系做扩展；第四步，对同一事件的多条来源按文档发布时间排序并去重，因为同一事件可能同时存在公告与多篇报道；第五步，把时间信息随证据一起送入证据融合与答案生成环节。

需要强调的是，"最新""最近""近期"这类相对时间表述一律以 event_time 判定，不以 publish_time 判定：最新取符合条件的、event_time 最大的事件，最近取 event_time ∈ [data_cutoff_time − 30 天, data_cutoff_time]，近期取 event_time ∈ [data_cutoff_time − 90 天, data_cutoff_time]，具体规则见 10.3 节；答案中需注明所用判定区间与数据截止时间。

如果缺少时间维度的建模，系统只能按语义相似度返回文本，无法回答"某时间之后发生了什么"，也无法区分事件发生与文档发布的时间差。因此答案中的每一条证据都需要标注其对应的时间类型，避免把文档发布时间当作事件发生时间；相对时间表述按 event_time 判定，而"某份材料何时公开"按 publish_time 判断，两者不得混用。对于示例中的情况，若用户询问的是公告发布之后公开的材料，2025-03-05 的公告与 2025-03-06 的报道都应被纳入；若用户询问的是 2025-03-01 至 2025-03-04 期间发生的事件，则只能命中 event_time 落在该区间的事件，不能被后续报道误导。

### 10.3 数据截止时间 data_cutoff_time

系统不是实时行情数据库，也不承诺数据持续更新，因此在时间维度中设置数据截止时间 data_cutoff_time，作为所有相对时间表述的基准。具体做法如下。

第一，data_cutoff_time 不属于任何表，而是数据集版本级属性，与 dataset_version 成对记录在数据集元信息中（配置文件 ＋ 数据集说明文档），例如 dataset_version = v1.0、data_cutoff_time = 2026-09-25。document 表不存放该字段，只记录每篇文档的入库时间 ingest_time；数据集边界是构建数据集时人为确定的版本属性，不再采用"取所有文档中的最晚时间"的推导口径。界面固定显示"数据截至：YYYY-MM-DD"；第 5 阶段封版的数据集 v1.0 取 2026-09-25，已据此填入，本文件不再以 YYYY-MM-DD 占位。

第二，"最新""最近""近期"等相对时间表述，一律以 event_time 判定，并以数据截止时间为基准解释，而不是以系统运行当天为基准，也不是以 publish_time 判定。判定规则如下表。

| 表述 | 判定规则 |
| --- | --- |
| 最新 | 符合条件的、event_time 最大的事件（并列时全部返回） |
| 最近 | event_time ∈ [data_cutoff_time − 30 天, data_cutoff_time] |
| 近期 | event_time ∈ [data_cutoff_time − 90 天, data_cutoff_time] |

例如用户询问"A 公司最近发生了哪些事件"时，系统按上表把"最近"解释为 event_time 落在数据截止时间前 30 天以内，并在答案中注明所用判定区间与数据截止时间，避免用户误以为系统掌握当下的实时信息。

第三，提示词、检索与答案生成一律以该版本属性为相对时间基准：data_cutoff_time 与 dataset_version 一起写入提示词，要求模型不得使用数据截止时间之后的信息，也不得在证据不足时用训练语料中的时间信息补全答案。

第四，论文与答辩中明确说明数据的时间边界：数据集覆盖的时间范围、数据截止时间、数据来源的发布时间分布都需要写清楚。这样处理可以避免答辩时被追问"你的数据最近到什么时候"，也可以避免把离线数据集包装成实时系统。

## 十一、技术路线

### 11.1 普通 RAG 流程

```text
财经文章
    ↓
切分
    ↓
Embedding
    ↓
FAISS

用户问题
    ↓
Embedding
    ↓
FAISS搜索
    ↓
相关文本
    ↓
LLM
    ↓
答案
```

### 11.2 KG-RAG 流程

```text
用户问题
    ↓
问题分类
    ↓
关键词/实体识别
    ↓
向量检索
    ↓
得到候选文本
    ↓
提取相关实体
    ↓
知识图谱扩展
    ↓
时间/关系过滤
    ↓
证据融合与排序
    ↓
LLM生成
    ↓
答案 + 来源 + 图谱关系
```

### 11.3 一次用户提问的完整执行流程

把 11.1 与 11.2 两条流程展开到系统实现层面，一次用户提问的完整执行过程如下。

```text
用户问题
   ↓
问题分类
   ↓
实体识别
   ↓
判断是否需要图谱
   ↓
┌───────────────┐
│               │
↓               ↓
向量检索       Neo4j 图谱检索
↓               ↓
文本证据       图谱路径
└───────┬───────┘
        ↓
    时间过滤
        ↓
    证据排序
        ↓
    Prompt 构建
        ↓
      LLM
        ↓
答案 + 来源 + 图谱路径
```

该图描述的是系统从收到问题到返回答案的完整链路：问题分类决定按哪类任务处理，实体识别找出问题中的公司与事件，判断是否需要图谱决定本次请求是否进入 Neo4j，向量检索与图谱检索并行或先后执行后得到文本证据与图谱路径，随后统一经过时间过滤与证据排序，再构建 Prompt 交给大模型，最终输出答案与来源；使用了图谱扩展时同时输出图谱路径，0 跳问题与未启用图谱的实验组标注"本次回答未使用图谱扩展"。

**时间过滤在这条链路中的位置（v2.5 澄清）**：时间过滤作用于**候选集合的成员**，即它按 event_time 过滤事件节点与关系，被过滤掉的图谱路径所携带的文本块不再进入候选。因此在实现层面，时间过滤必须发生在"两路候选合并与去重"**之前**：若把它放在"保留到 K 个文本块"之后，C 组与 D 组的最终证据集合将恒等，四项检索指标也随之恒等，C→D 的对比（即 H3 的检验）就不包含任何可观察的差异。这一澄清不改变本图自上而下的箭头顺序（时间过滤在图谱检索之后、证据排序之前），只把它与 第12.7节 五步契约的先后关系写明。

这张图是论文第四章与答辩 PPT 的核心图：论文的系统设计章节用它说明系统业务流程与模块协作关系，答辩时用它说明"系统到底做了什么、知识图谱在什么位置起作用、时间过滤发生在哪一步"。后续第 5 章与第 6 章的实现与实验描述都以这张图为骨架展开。

### 11.4 整体技术路线

两条流程在实际开发中合并为一条完整的实现路线：

```text
财经文本
    ↓
数据清洗
    ↓
实体/事件/时间抽取
    ↓
事件知识图谱
    ↓
文本向量化
    ↓
向量索引
    ↓
用户问题
    ↓
问题分析
    ↓
向量检索
    ↓
图谱扩展
    ↓
时间与关系过滤
    ↓
证据融合
    ↓
大语言模型
    ↓
答案
    ↓
证据 + 来源 + 图谱路径
```

该图沿用 v1.0 的节点与顺序，仅按 v2.0 的术语口径把原来标注为"向量数据库"的一栏统一改为"向量索引"，其余内容与 v1.0 一致。整条路线可以压缩成一句话：先准备数据，再抽取事件与构建图谱，同时为文本建立向量索引，用户提问时先用向量检索找到文本证据，再用图谱扩展补充关系，经过时间过滤与证据融合后交给大模型生成带来源与图谱路径的答案。

### 11.5 开发纪律：必须先跑通普通 RAG，再加知识图谱

开发顺序必须遵守一条纪律：先完成普通 RAG 并跑通，再接入知识图谱。普通 RAG 是最小可运行闭环，只有它能够稳定回答问题，后续加入图谱后产生的效果差异才有比较基准；同时，普通 RAG 本身就是 Baseline 2 与消融实验 A 组的实现，提前做好可以直接复用。

如果一开始就把数据、图谱、检索、前端与大模型同时搭建，一旦结果不正确，就无法判断问题出在 RAG 检索、知识图谱构建、前端展示还是大模型生成上，排查成本会成倍增加。因此本课题把普通 RAG 作为里程碑一的核心内容，在它稳定之后再进入事件抽取与知识图谱阶段。

## 十二、混合检索策略与实验方案

### 12.1 检索策略分级

| 策略 | 定义 | 主要对应的问题类型 |
| --- | --- | --- |
| 0跳 | 只进行文本检索 | 事实型问题 |
| 1跳 | 文本检索 + 一跳图谱关系 | 公司直接关系问题，如供应商、客户、竞争对手、高管 |
| 2跳 | 文本检索 + 两跳图谱关系 | 关联方事件、上下游间接关系问题 |
| 时间过滤 | 文本检索 + 图谱 + 时间约束 | 时序问题与"某时间之后"的问题 |

### 12.2 测试集（多标签）

v2.0 取消 v1.0 的六分类表格，改为多标签体系。每道题打三个标签：任务类型（事实型／事件型／关系型）、路径深度（gold_hop_depth：0-hop／1-hop／2-hop）、时间约束（无／有）。测试集规模为：完整版 120 题（核心 108 + 压力 12），缩减版 60 题（核心 54 + 压力 6）。

路径深度标签使用 question 表的 gold_hop_depth 字段（标注路径深度），定义为：该问题在客观上最少需要沿几条图谱关系才能完整得到答案。判定规则如下。

- 0-hop：仅凭原始文本证据即可完整回答，不需要任何图谱关系。
- 1-hop：至少需要沿 1 条图谱关系才能完整回答。
- 2-hop：至少需要沿 2 条图谱关系才能完整回答。

标注约定：以"最少必需证据"为判据；存在多条可行路径时取最少跳数；若图谱中虽有关系但文本证据已足以回答，标 0-hop。必须明确区分：gold_hop_depth 是问题的金标准属性；消融实验 B／C 组的 1-hop／2-hop 是实验自变量（系统的检索深度）。两者不得使用同一字段、同一术语或同一统计口径；原设计把两者都叫"路径深度"，是方法泄漏风险来源，已在 v2.1 中消除。

完整版 120 题的构成为：9 个"任务类型 × 路径深度"组合各 12 题（3 × 3 × 12 = 108 题），构成核心测试集；另加 12 题"关系型 + 2 跳 + 有时间约束"压力测试子集，合计 120 题。缩减版 60 题的构成为：9 个组合各 6 题（核心 54 题），另加 6 题同一压力测试子集，合计 60 题。

| 部分 | 构成 | 报告方式 |
| --- | --- | --- |
| 核心测试集 | 9 个"任务类型 × 路径深度"组合各 12 题 = 108 题 | 主结果以 108 题为准 |
| 压力测试子集 | 12 题"关系型 + 2 跳 + 有时间约束" | 单独报告，不并入 108 题的总体平均 |

数据集规模处可以写"120 题（核心 108 + 压力 12）"，但任何统计量必须注明统计范围（108 或 12），不得出现把两者混算的平均值。压力子集可作为附加观察参与讨论，但不作为 12.8 节主体判定的依据。压力测试子集中"关系型 + 2 跳 + 有时间约束"的 12 题在标签上落入 9 个组合中的"关系型 + 2 跳"一格，若并入总体平均相当于对该组合额外加权、会把总体平均带偏，因此单独报告。每个"任务类型 × 路径深度"单元格内，有时间约束与无时间约束的题目各占一半（完整版 6 + 6，缩减版 3 + 3），使时间约束这一维度在每个单元格上完全平衡，避免时间标签对某一类问题形成偏置。

| 任务类型 × 路径深度 | 完整版题量 | 缩减版题量 | 时间约束标签（完整版／缩减版） |
| --- | --- | --- | --- |
| 事实型 + 0 跳 | 12 题 | 6 题 | 无 6 题 + 有 6 题（缩减版 无 3 + 有 3） |
| 事实型 + 1 跳 | 12 题 | 6 题 | 无 6 题 + 有 6 题（缩减版 无 3 + 有 3） |
| 事实型 + 2 跳 | 12 题 | 6 题 | 无 6 题 + 有 6 题（缩减版 无 3 + 有 3） |
| 事件型 + 0 跳 | 12 题 | 6 题 | 无 6 题 + 有 6 题（缩减版 无 3 + 有 3） |
| 事件型 + 1 跳 | 12 题 | 6 题 | 无 6 题 + 有 6 题（缩减版 无 3 + 有 3） |
| 事件型 + 2 跳 | 12 题 | 6 题 | 无 6 题 + 有 6 题（缩减版 无 3 + 有 3） |
| 关系型 + 0 跳 | 12 题 | 6 题 | 无 6 题 + 有 6 题（缩减版 无 3 + 有 3） |
| 关系型 + 1 跳 | 12 题 | 6 题 | 无 6 题 + 有 6 题（缩减版 无 3 + 有 3） |
| 关系型 + 2 跳 | 12 题 | 6 题 | 无 6 题 + 有 6 题（缩减版 无 3 + 有 3） |
| 压力测试子集：关系型 + 2 跳 + 有时间约束 | 12 题 | 6 题 | 有 |
| 合计 | 120 题（核心 108 + 压力 12） | 60 题（核心 54 + 压力 6） |  |

之所以改为多标签，是因为 v1.0 的六分类彼此重叠，会导致分组统计口径歧义。例如"某公司被处罚后其供应商有哪些相关事件"这一道题，同时属于事件问题、关系问题、两跳问题、时序问题与综合问题，在六分类体系下只能被放进其中一类，一旦放入某一类，其他类别的统计就会漏掉这道题，不同的人在复现实验时也可能给出不同的归类结果。采用多标签之后，每道题可以同时计入多个子集，分组分析才严谨：同一道题既进入"关系型"子集，也进入"2 跳"子集，还进入"有时间约束"子集，各子集之间允许重叠，统计时分别按子集计算指标，不再需要人为决定"这道题主要算哪一类"。

测试集中每道题记录以下内容：题干、三个标签、参考答案、应命中的证据文档编号与文本块编号。证据以 document 与 document_chunk 的编号记录，与第 9.1 节的映射链路一致，这样检索指标与证据召回指标可以直接计算，不需要在实验阶段重新人工判断"应该命中哪些文档"。测试集从真实财经文本出发构建，题目、参考答案与证据编号在实验开始前固定，对比实验与消融实验重复使用同一套题目，避免不同实验使用不同题目导致结果不可比。若进度紧张，按 12.2 节的缩减版执行，先把 60 题（核心 54 + 压力 6）做扎实，再按同一套标签规则扩充到 120 题（核心 108 + 压力 12），但不得改变标签体系与题目记录格式。

每道测试题标注的 gold 证据文本块数量不得超过实验设定的 K（Top-K 的取值，见 12.4 节）。若某题的原始证据数量超过 K，则在测试集构建阶段合并相邻证据块或重新定义证据粒度，使 Complete Evidence Recall@K 在定义上可达；不得保留 gold 证据数大于 K 的题目，否则该题的这一指标必然为 0，会系统性压低指标并造成误判。

### 12.3 抽取评测集

抽取评测集用于验证 RQ1，回答"财经事件能否被稳定地结构化表示"这一问题。评测集分为开发集 Dev 与测试集 Test 两段，从最终数据集中抽取，规格与规则如下表。

| 集合 | 规模 | 用途与规则 |
| --- | --- | --- |
| 开发集 Dev | 50～100 条（建议 60 条） | 从最终数据集中抽取，用于抽取规则与 Prompt 的迭代调优；调优过程在论文中说明 |
| 测试集 Test | 200 条 | 独立于 Dev，在抽取规则与 Prompt 冻结后一次性评测，只测一次；Test 结果不得反馈用于再调优 |

两集合不得重叠，均记录文本编号；若确需修改规则，必须另行划定新的 Test 并在论文中说明，不得用原 Test 反复调参。标注总量约 250～300 条，计入"事件抽取与知识图谱"阶段的工期。

**"条"的口径（v2.7 澄清）**：本节所称"条"指**标注条目**，其单位是**文本块**（一个文本块一条），不是整篇文档。理由是：抽取评测要把金标准对齐到证据的最小单位，而 第12.7节 已把文本块定为"证据的最小单位"；若按"篇"计，250～300 条将要求同等数量的文档，与 第17节 的第一版 100 篇／第二版 500 篇口径无法自洽。据此，Dev 与 Test 的"不重叠"指**文本块不重叠**，Dev 与 Test 各自记录的"文本编号"即 `chunk_id`。第一版数据集已有 707 个文本块，容量充分。

标注内容为由作者人工标注六类实体、事件类型与论元、关系以及时间信息，作为金标准，然后用系统抽取结果与金标准对比，分别计算实体、事件、关系、时间四类任务的 Precision、Recall 与 F1。

评测集的字段与 9.2 节的本体保持一致：实体标注 Company、Person、Industry、Institution、Event、Policy 六类标签；事件标注 event_type、event_time 与参与主体及其角色；关系标注 9.2.3 节的 9 条核心关系及其方向；时间标注事件发生时间与文档发布时间。标注过程中发现的本体边界问题（例如某条文本中的关系无法归入 9 条核心关系）记录在标注说明中，用于在论文中讨论本体的适用边界，而不是临时增加关系类型。

没有抽取评测集，RQ1 无法成立：如果没有金标准，抽取质量只能靠零散的例子说明，无法给出可核查的数字，也无法解释后续问答实验中出现的错误有多少来自抽取环节。因此 Dev 与 Test 的人工标注工作必须完成，标注总量约 250～300 条，并计入"事件抽取与知识图谱"阶段的工期。标注完成后，评测集（Dev 与 Test）与被标注文本的编号一并保存在数据集目录中，作为交付物的一部分。

### 12.4 实验配置固定表

实验开始前必须固定下表七项配置，实验期间不得更改；模型名称与版本在选型完成后填入并固化。截至 v2.8，**Embedding 模型与版本、dataset_version／data_cutoff_time 与切分参数已在第 5 阶段（数据准备）固化**，**大语言模型与版本、Top-K（K）与 Context Token Budget 仍为 TBD**，由后续阶段按本表的归属固化。

| 配置项 | 取值 | 说明 |
| --- | --- | --- |
| Embedding 模型 | **BAAI/bge-small-zh-v1.5**（第 5 阶段固化） | 负责文档文本块与用户问题的向量化，实验期间保持不变。512 维，L2 归一化后以 FAISS `IndexFlatIP` 建索引（内积＝余弦相似度），CPU 可跑；所有对比组与消融组共用同一模型。选型理由与落地细节见《13-数据准备（第五阶段）》第四节 |
| 大语言模型 | TBD（选型后固化） | 负责基于证据生成答案，实验期间保持不变 |
| 模型版本 | Embedding：**`7999e1d3359715c523056ef9478215996d62a620`**（HuggingFace revision，第 5 阶段固化）；大语言模型版本仍 TBD（选型后固化） | 模型名称与版本号一并记录，避免接口侧静默升级导致结果不可复现。Embedding 的 revision 已写入 `meta\dataset.json` 与 `index\build_meta.json`，第 7 阶段建索引前核对；换 revision 等于换数据集版本 |
| temperature | 0 | 固定为 0，降低生成随机性，保证同一问题在相同证据下的输出尽可能稳定 |
| Prompt 版本 | v1.0 | 提示词模板统一编号，实验中所有对比组使用同一版本 |
| Top-K（K） | TBD（预实验后固化） | **每个问题最终证据集合的文本块数量上限**，同时就是 12.7 节四项检索指标里的 K；向量检索的候选数量 N（N ≥ K）与 Context Token Budget 与 K 在预实验中一起固定，实验期间保持不变。完整的形成规则见 12.7 节 |
| Context Token Budget | TBD（预实验后固化） | 每次送入大模型的证据上下文（文本块＋图谱路径＋事件三元组）合计不超过同一 token 上限 |

系统支持多模型切换，Embedding 模型与大语言模型都通过配置项接入，更换模型不需要修改前端代码。但实验期间必须固定同一套模型、参数与 Prompt：所有对比组与所有消融组使用相同的 Embedding 模型、相同的大语言模型与版本、相同的 temperature、相同的 Prompt 版本、相同的 Top-K 与相同的 Context Token Budget，否则实验不可复现，指标差异也无法归因到知识图谱这一变量上。每次实验运行记录配置快照与运行时间，配置快照随实验结果一并保存。

上下文预算的裁剪规则必须固定并写入论文：证据总量超出预算时，按固定优先级裁剪——先裁剪与问题实体无关的远端图谱路径，再按**向量检索的原始排名**从后往前裁剪文本块。关键约束是：裁剪必须发生在"证据排序"这一实验变量**之前**，且裁剪依据不得使用 E 组的证据排序结果，否则 D 组与 E 组最终送入模型的证据集合会不同，"E 只改变顺序"就不再成立。正确顺序是：候选证据生成 → 按固定规则裁剪到预算之内 → 得到 D 组与 E 组**完全相同**的证据集合 → D 组按固定的原始顺序送入大模型，E 组只在这一集合内部重新排序。所有对比组与消融组使用同一预算与同一裁剪规则。理由是：若不控制上下文预算，KG-RAG 会天然获得更多 token，无法把效果差异归因到"图谱结构"这一变量；若只规定不超过同一 token 上限而不规定超限时裁掉什么，不同组的裁剪结果同样不可比。

**注记（v2.8，第 6 阶段收口时追加，依据《15-第6阶段任务书（事件抽取与知识图谱）》第2.4节（二））**：**抽取所用的模型、模型版本与 Prompt 版本属于上游数据生产参数**——它们决定的是"图谱里有什么"，不是"答案怎么生成"，因此**不并入本表的七项**。本表的「大语言模型」一行指的是**答案生成模型**，由第 8 阶段固化，保持 TBD。本条注记**不改变七项的计数，也不改动七项中任何一行的取值**；抽取侧三项参数的冻结值与选型依据登记在《16-事件抽取与知识图谱（第六阶段）》第一节，重放抽取结果时按该处的版本使用同一份缓存（《15》第五节 硬约束 7～10）。本注记的登记不使 第12.4节 的表变成八项，也不改变本节任何一行对答案生成侧配置的归属。

### 12.5 实验设置

| 编号 | 设置 | 说明 |
| --- | --- | --- |
| Baseline 1（闭卷 LLM，辅助基线） | 直接调用 LLM，不接入检索 | 辅助基线；因其依赖参数化知识且与数据集的时间边界不一致，不作为"KG-RAG 有效"的主要证据 |
| Baseline 2 | 普通 Vector RAG | KG 是否提供额外价值的主要对照 |
| Method | Vector RAG + 2-hop Event KG（**即 12.6 节的 C 组**） | 本课题方法。为使 Baseline 2 与 Method 的对比只改变"是否使用事件知识图谱"这一个变量，Method 采用 2-hop 图谱扩展，**不启用时间过滤、不启用 E 组的证据排序**；系统本身支持 D／E 两组的配置，那两组作为消融与进一步优化实验单独报告，不计入 Method 的定义 |

三组设置使用同一套测试集与同一评测口径，用于回答"是 LLM 本身有效、是 RAG 有效，还是知识图谱确实提供了额外价值"。**三组实验与消融 A～E 的对应关系必须写清楚：Baseline 2 ＝ A 组（Vector RAG），Method ＝ C 组（Vector RAG + 2-hop KG）。**因此 Baseline 2 与 Method 的对比等价于 A 与 C 的对比，唯一变量是"是否使用事件知识图谱"；在它之上再加时间过滤得到 D、再加证据排序得到 E，那两步的贡献分别由 C→D 与 D→E 回答，不混入 H1 的判定。回答 RQ2／H1 的主要证据是 Baseline 2 与 Method 的对比，Baseline 1 仅作辅助观察：它依赖参数化知识，且其训练语料的时间边界与数据集不一致，不能作为"KG-RAG 有效"的主要证据。三组实验的结果都按 12.2 节的多标签子集分别统计，而不是只给一个总体平均值：总体平均值会把不同难度、不同类型的题目混在一起，掩盖知识图谱在关系型、多跳型与时序型问题上的作用，也无法回答 H1 提出的问题。

### 12.6 消融实验

| 编号 | 配置 | 继承关系 | 目的 |
| --- | --- | --- | --- |
| A | Vector RAG | 基线 | 作为消融基线 |
| B | Vector RAG + 1-hop KG | A + 1-hop 图谱扩展 | 观察一跳图谱关系的贡献 |
| C | Vector RAG + 2-hop KG | A + 2-hop 图谱扩展（含 1-hop 结果） | 观察两跳关系扩展的贡献 |
| D | Vector RAG + 2-hop KG + 时间过滤 | D = C + 时间过滤，其余不变 | 观察时间约束的贡献 |
| E | Vector RAG + 2-hop KG + 时间过滤 + 证据排序 | E = D + 证据排序，其余不变 | 观察证据融合与排序的贡献 |

五组实验使用同一测试集、同一大模型、同一 Prompt 模板、同一 Context Token Budget 与同一裁剪规则、同一评分标准，逐项增加模块，记录每一步的效果变化。继承关系必须明确：C ⊆ D ⊆ E，D 在 C 的基础上只增加时间过滤，其余不变；E 在 D 的基础上只增加证据排序，其余不变。所有对比结果需要保留原始输出，便于在论文中给出可核查的数据。

关于路径深度的口径需要澄清：这里的 1-hop 指查询目标实体的直接关系，例如从 A 公司出发沿一条关系边找到其供应商；2-hop 指允许沿两条关系边扩展，例如从 A 公司出发先找到供应商，再沿第二条关系边找到供应商参与的事件或其他关联企业。2-hop 的检索结果包含 1-hop 的结果，也就是说 C 组是在 B 组的基础上继续扩展，而不是把 B 组的结果排除之后只看第二条边新引入的部分。这一口径决定了 B 组与 C 组的指标差异表示"在直接关系基础上继续扩展两跳带来的变化"，而不是"两跳独有的关系带来的变化"，在论文中必须明确写出。同时必须强调：B／C 组的 1-hop／2-hop 是实验自变量（系统的检索深度），与 12.2 节 question 表的 gold_hop_depth（标注路径深度，问题的金标准属性）不是同一概念，不得使用同一字段、同一术语或同一统计口径。

A～D 四组对应 RQ2 与 RQ3：A 为基线，B 与 C 观察系统检索深度的影响（对应 H2）；H3 由 C 与 D 的对比回答，因为这一对比的唯一变量是时间过滤，且两组的检索深度都固定为 2-hop；A 与 C 的对比**就是** 12.5 节 Baseline 2（＝A）与 Method（＝C）的对比，它为 RQ2／H1 提供主要证据，与三组对比实验是同一件事的两种表述，论文中不得写成两组互不相干的证据。

E 组单独列为"进一步优化实验"，不归入 RQ3。E = D + 证据排序：上下文预算的裁剪在排序之前完成，因此 D 与 E 使用**完全相同**的证据集合，E 只改变该集合内部的顺序与呈现顺序，不改变证据集合本身，也不改变图谱检索路径的深度，也不改变时间约束是否启用。如果把它并入 RQ3，就会把"图谱结构与时间约束带来的效果"与"排序策略带来的效果"混在一起，无法判断差异的来源。因此本课题在论文中把 E 组与 A～D 分开报告，作为一个独立的优化实验说明证据融合与排序的作用，并在结论中明确它不属于 RQ3 的研究范围。

### 12.7 评价指标与定义

评价指标分为检索指标、问答指标与系统指标三组，每一项都给出明确定义，评分标准在实验开始前固定。

检索指标四项，主口径统一为文本块级（document_chunk）：向量索引检索的单位是文本块，且测试集已记录 gold 证据的 document 与 chunk 编号，因此不再混用文本块级与文档级两种口径。定义如下。Recall@K（chunk 级）：Top-K 中命中的 gold 证据文本块数 ÷ 该问题 gold 证据文本块总数，按问题取平均。Precision@K（chunk 级）：Top-K 中属于 gold 证据的文本块数 ÷ K，按问题取平均。MRR（chunk 级）：第一个 gold 证据文本块所在排名的倒数，按问题取平均；前 K 内无 gold 证据则该问题记 0。Complete Evidence Recall@K（问题级）：该问题的 gold 证据文本块全部出现在 Top-K 中记 1，否则记 0，按问题取平均（原"Evidence Recall"更名为本名）。

两个层次的分工必须写明："召回了多少"由 Recall@K 承担，"是否全部召回"由 Complete Evidence Recall@K 承担，因此不再另设同义指标。文档级只在需要诊断时作为辅助口径使用，且必须在论文中单独标注，不得与 chunk 级混算。

**Top-K 证据集合的形成规则必须固定，四项检索指标都在这个集合上计算。**规则共五步。第一步，文本候选来自两路：向量检索按配置项返回候选文本块（候选数量记为 N），图谱扩展按关系的 source_chunk_id 指向的文本块并入候选。**D 组与 E 组另在本步对候选集合施加时间过滤（按组启用，规则见 第11.3节）；时间过滤作用于候选集合的成员，必须先于第二步的合并与去重**，否则 C 组与 D 组的最终证据集合将恒等、H3 失去可观察差异。N 与 K 在预实验中一起固定，且保证 N ≥ K。第二步，两路候选统一映射到 chunk_id 并按 chunk_id 去重：**同一个文本块无论被哪一路命中都只算一个证据**，重复命中既不重复计数、也不合并加权，避免同一个 chunk 在 Precision 与 Recall 中被算两次。第三步，去重后的候选集合按 12.4 节的固定裁剪规则裁剪到 Context Token Budget 之内；该规则与 K 的关系是——"先裁剪与问题实体无关的远端图谱路径"作用于图谱路径与事件三元组部分，"再按向量检索的原始排名从后往前裁剪文本块"就是本节的"按向量原始排名保留文本块"，其保留上限即 K。第四步，**保留到 K 个文本块这一步必须发生在 D／E 分组排序之前**，因此 D 组与 E 组得到**完全相同**的最终证据集合（大小不超过 K），E 组只改变该集合内部的顺序——若在排序之后才截取到 K，D 与 E 截出的前 K 个就可能不同，"E 只改变顺序"将不再成立。**本步与第一步的分工必须分清（v2.5 澄清）**：第一步的时间过滤作用于候选集合的成员，因此 C 组与 D 组的最终证据集合**可以不同**，这是 H3 可测的前提；本步的截取位置保证 D 组与 E 组的最终证据集合**必然相同**，这是 E 组单变量的前提。两条约束作用在不同的组对上，互不冲突，不得因为"E 只改变顺序"而把时间过滤也放到截取之后。第五步，最终证据集合既是四项检索指标的计算对象，也是送入大模型的文本证据上下文；图谱路径与事件三元组计入 Context Token Budget，但不计入检索指标的 K（检索指标只统计文本块级证据）。为保证 Precision@K 的分母为 K 且可跨题比较，预实验需固定 K、N 与 Context Token Budget，并保证每个问题的候选证据数不少于 K、预算至少能容纳 K 个文本块；候选证据不足 K 的题目在测试集构建阶段调整，不进入正式实验。**该约束只适用于测试集构建阶段（v2.5 澄清其边界）**：题目一旦入库，不得因该题实验时的实际候选数少于 K 而将其排除，空缺位置记为未命中并按原指标定义计算；否则会出现"系统没搜到 K 个就把题删掉"的选择偏差，使正式测试集因系统表现差而变简单。

问答指标三项，由人工按统一标准评分，每项取值为 0、1、2。Answer Accuracy：0 = 错误，1 = 部分正确，2 = 完全正确。Completeness：0 = 关键信息大量遗漏，1 = 基本覆盖，2 = 信息完整。Faithfulness：0 = 存在明显无依据事实，1 = 少量不严谨，2 = 完全基于证据。

评分方式：由作者按上述统一标准评分，并对不少于 20% 的样本间隔 3 天以上做二次复评，报告前后评分一致率，用于检验评分稳定性（intra-rater consistency）。两次评分不一致的样本由作者重新核对证据后给出最终分，并在实验记录中保留两次评分与不一致原因。本课题以单人复评（评分稳定性）作为既定设计，不得表述为"评分者一致性"或"评分信度"——单人复评无法证明评分者间信度（inter-rater reliability）。条件增强：若能有第二名评分者，则对同样的 20% 样本独立评分，计算加权 Cohen's Kappa（线性权重，0/1/2 为序数尺度），作为评分者间信度证据；无第二名评分者时不得声称信度。评分标准在实验开始前固定，不得在看到结果之后调整。若确实需要修改评分标准，必须在修改后重新执行全部评分，并在论文中说明修改原因与重评范围。

系统指标沿用第 7 章非功能需求的口径，并补足一项：响应时间一项沿用第 7 章"在统一的测试环境下记录平均回答耗时与 P95 耗时，并分别记录向量检索、图谱查询与大模型生成的耗时"的记录方式；并发能力一项沿用第 7 章"10 个并发用户下接口成功率不低于 95%"；此外本节新增一项补充口径"连续 100 次请求的成功率不低于 95%"，用于观察非并发条件下的接口稳定性，该项**不属于**第 7 章已有条目。两项口径均以"系统不崩溃、不出现数据错乱"为前提。三组指标同时覆盖检索方法、问答质量与软件系统表现，既满足方法层面的评价，也满足软件工程专业的系统评价要求。

### 12.8 图谱增强效果的判定规则

为了避免"看到结果再决定怎么解释"，知识图谱是否提供额外检索价值按以下规则判定，且**判定按子集分别进行**：对**关系型子集**（任务类型为"关系型"的题目）、**多跳型子集**（路径深度为 1-hop 或 2-hop 的题目，即 gold_hop_depth ≥ 1）、**时序型子集**（时间约束为"有"的题目）各自独立应用同一条规则——若 KG-RAG 在该子集上的 Complete Evidence Recall@K 高于 Vector RAG，且该子集的 Answer Accuracy 不下降（持平或提升），则判定知识图谱在该类问题上提供了额外检索价值。三个子集按 12.2 节的标签定义，**允许重叠**（同一道题可以同时属于多个子集）；"互不牵连"指判定与结论各自独立、不因一个子集的结果而改变另一个子集的判定，不指三个集合互斥。可以只有一个子集满足、两个满足或三个都不满足，不得把三个子集合并成一个整体来判定，否则"知识图谱只对某类问题有效"这一可能结果会被整体判定掩盖。该判定以核心测试集的 108 题为依据，压力测试子集仅作为附加观察参与讨论，不作为本节主体判定的依据。

该规则只规定判定方式，不预设提升幅度。本课题不预设"知识图谱一定能提升多少"，也不预设"哪一类问题上必然有效"：实验可能出现三种结果，一是多个子集上同时满足判定条件，说明知识图谱在这些子集上提供了额外价值；二是只有部分子集满足判定条件，说明知识图谱的作用集中在特定类型的问题上，论文按子集分别报告；三是没有任何子集满足判定条件，说明在该数据集与配置下知识图谱没有带来可观察的额外价值，这一结果同样按 12.7 节的指标如实报告，并按第十八章的分类归因方法分析原因。三种结果都是有效的实验结论，区分点在于是否用同一套指标、同一套配置与同一套判定规则给出结论。

### 12.9 定量实验问题与开放性问题的区分

定量实验只使用可核验、有明确证据的事实型问题，即答案能够在数据集文档中找到直接依据、可以人工判定正确与否的问题。示例包括：A 公司的供应商有哪些；A 公司什么时候发生该事件；A 公司参与了哪些事件；A 公司与 B 公司是什么关系；某事件之后又发生了哪些事件。这类问题有唯一的证据集合与可核查的参考答案，适合计算 Recall@K、Precision@K、MRR、Complete Evidence Recall@K 与 Answer Accuracy，也适合作为多标签子集分组统计的对象。

开放式分析问题不进入定量实验，只用于 Demo 展示与案例分析，最多保留 1～2 例。示例包括：A 公司被处罚后对 B 公司的潜在影响；该政策对某行业的长期影响如何判断。这类问题没有唯一答案，回答质量依赖模型的分析角度与措辞，无法用 12.7 节的 0/1/2 评分稳定衡量；同时这类回答必然包含模型分析成分，容易与公开事实混淆。因此这类问题必须在页面与答案中同屏标注"模型分析（非公开事实）"，只用于答辩演示、页面展示与失败案例分析，不作为核心准确率依据，也不参与 12.8 节的判定，不计入任何指标。

答辩演示与页面展示以事实型、事件型、关系型、时序型四类可核验问题为主，每类至少 2 例；上述开放式分析问题最多保留 1～2 例，并必须按前一段的要求标注"模型分析（非公开事实）"，不计入任何指标。

区分的意义在于保证实验结论可核查：定量实验部分报告的数字都有明确证据与人工参考答案支撑，开放式部分只展示系统的表达能力与证据追溯能力，不把开放性回答的效果折算成准确率。论文中两部分分开报告，实验章节给出定量结果，案例分析章节给出开放性问题与失败案例。

## 十三、最终回答形态与证据追溯

系统输出不能只有一句结论，必须同时给出答案与可追溯证据。这里需要区分两种情形：对于使用了知识图谱检索的问题（1 跳或 2 跳扩展），答案必须同时展示对应的图谱路径；对于 0 跳问题（仅凭文本检索即可回答）以及未启用图谱扩展的实验组（Baseline 1、Baseline 2 与消融 A 组），客观上不存在图谱路径，此时页面与答案中显式标注"本次回答未使用图谱扩展"，**不得强行生成或展示虚构的图谱路径**。以供应链关联为例（该例使用了图谱扩展），最终回答形态如下：

```text
回答：

A公司于XXXX年发生监管处罚事件。

相关公开信息显示，A公司与B公司存在供应链关系。

因此，从公开信息关联角度看，B公司可能属于需要进一步关注的相关企业。

证据来源：

[1] A公司监管处罚公告
[2] A公司年度报告
[3] B公司公告

知识图谱路径：

A公司
  ↓
供应商关系
  ↓
B公司
```

其中，每条证据都需要能够定位到具体文档，展示来源类型（公告或新闻）、发布时间与文档编号，用户可以点击查看原文；图谱路径以可视化方式展示，说明答案中的关系是如何被检索到的；若本次回答未使用图谱扩展（0 跳问题或未启用图谱的实验组），该区域显示"本次回答未使用图谱扩展"，而不是留空或展示虚构路径。"因此，从公开信息关联角度看"这类表述属于模型分析，必须与前面的公开事实明确区分，不能写成由图谱关系直接推导出的因果结论。

回答页面同时固定显示"数据截至：YYYY-MM-DD"，并向用户说明"最新／最近／近期"类问题的判定基准是数据截止时间而不是系统运行当天，并一律按 event_time 判定，答案中同时注明所用的判定区间。证据列表中的每条记录都来自 answer_evidence 表，包含文档编号与文本块编号，因此可以沿着 9.1 节的映射链路一路定位到原始文章；图谱路径中的每条关系都可以回溯到 9.2.3 节定义的 9 条核心关系之一；其中除 EVIDENCED_BY 外的 8 条核心语义关系带有 source_doc_id 与 source_chunk_id，指向产生该关系的文档与文本块，confidence 表示抽取置信度；EVIDENCED_BY 不带这三项属性，其证据就是该关系指向的 Document 节点本身。

这类带证据的回答是本系统的重点展示功能，也是答辩现场演示的主要流程：输入问题、得到答案、查看证据、查看图谱关系。演示问题以事实型、事件型、关系型、时序型四类可核验问题为主（每类至少 2 例），开放式分析问题最多保留 1～2 例，并必须在页面与答案中同屏标注"模型分析（非公开事实）"。

## 十四、主要工作与特色

v1.0 中本节标题为"创新点"，v2.0 改为"主要工作与特色"，三条内容也相应改写为工作性表述。在本科毕业论文中，把该节写成"主要工作与特色"比自称"三项创新"更稳妥：本科阶段的工作大多是已有方法的组合与工程化实现，直接声称"创新"容易被追问创新点与已有工作的边界，而按"主要工作与特色"表述则如实说明自己做了什么、这些工作有什么特点，评审更容易接受。

### 特色一：面向财经事件的事件中心化知识表示

针对传统实体关系图谱难以完整表达财经事件时间、参与主体和来源信息的问题，引入事件作为核心知识单元，实现实体、事件、关系、时间及证据的一体化表示。具体做法是：事件作为独立节点承载 event_id、event_type、event_name、event_time、description 与 confidence 六项核心属性，事件类型固定为 8 种核心事件类型并作为抽取、标注与评测唯一的 schema；参与主体通过 PARTICIPATES_IN 关系及其 role 属性表达（定义域为 Company／Person／Institution → Event，role ∈ {主体, 合作方, 涉及方, 监管方, 受影响方}），政策事件与监管事件的发布／作出机构通过 ISSUED_BY 关系表达；除 EVIDENCED_BY 外其余 8 条核心语义关系均携带 source_doc_id、source_chunk_id 与 confidence 证据属性，事件与文档之间通过 EVIDENCED_BY 关系关联，支持一个事件挂多份文档；公司以 stock_code 作为稳定业务唯一标识，并通过实体消歧与事件去重保证图谱中同一实体、同一事件只出现一次。

### 特色二：融合实体关系与时间约束的知识图谱增强混合检索方法

在传统向量检索基础上，引入事件知识图谱进行实体关系扩展，并结合关系类型和时间约束筛选相关证据，形成面向财经事件问答的混合检索方法。检索流程按问题分类、实体识别、是否需要图谱、向量检索与图谱检索、时间过滤、证据排序的顺序组织；系统的检索深度分为 0 跳、1 跳、2 跳，与 12.2 节问题的 gold_hop_depth 金标准标签区分开；时间约束作为独立环节参与过滤，"最新／最近／近期"等相对时间表述以 event_time 判定；所有对比组与消融组使用同一 Context Token Budget 与同一裁剪规则；知识图谱的加入不改变文本证据的基础地位，而是补充文本检索难以覆盖的关系与时间信息。

### 特色三：面向财经事件多跳问答的证据追溯与系统化评测机制

系统为每条答案保留证据文档与文本块，并对使用了图谱扩展的回答保留图谱路径，使回答可以逐条回溯到原始材料（0 跳回答显式标注未使用图谱扩展）；实验部分构建多标签测试集（完整版 120 题＝核心 108 + 压力 12，缩减版 60 题＝核心 54 + 压力 6）与两段式人工标注的抽取评测集（Dev 50～100 条，建议 60 条；Test 200 条；合计约 250～300 条），检索指标统一为 chunk 级主口径，配合抽取实验、对比实验、消融实验与五类失败案例归因，形成从抽取质量到问答效果的完整评测链条。"构建 120 题测试集"不作为单独的特色条目列出，它属于实验方案的一部分，已在 12.2 节详细说明。

## 十五、论文目录

### 第一章 绪论

- 1.1 研究背景
- 1.2 研究意义
- 1.3 国内外研究现状
- 1.4 研究内容
- 1.5 论文组织结构

### 第二章 相关技术

- 2.1 大语言模型
- 2.2 RAG 技术
- 2.3 向量检索
- 2.4 知识图谱
- 2.5 事件抽取
- 2.6 Neo4j
- 2.7 FastAPI
- 2.8 前端相关技术

### 第三章 系统需求分析

- 3.1 用户角色分析
- 3.2 功能需求
- 3.3 用例分析
- 3.4 非功能需求
- 3.5 可行性分析

### 第四章 系统设计

- 4.1 系统总体架构
- 4.2 系统功能模块设计
- 4.3 系统业务流程
- 4.4 数据库设计
- 4.5 知识图谱本体设计
- 4.6 RAG 检索架构
- 4.7 接口设计

### 第五章 系统实现

- 5.1 数据处理模块实现
- 5.2 事件抽取实现
- 5.3 实体消歧与事件去重实现
- 5.4 知识图谱构建实现
- 5.5 向量索引构建实现
- 5.6 混合检索实现
- 5.7 大模型问答实现
- 5.8 证据追溯实现
- 5.9 前端页面实现

### 第六章 系统测试与实验分析

- 6.1 测试环境与实验配置
- 6.2 评价指标定义与评分标准
- 6.3 抽取实验
- 6.4 功能测试
- 6.5 接口测试
- 6.6 性能测试
- 6.7 检索实验
- 6.8 问答对比实验
- 6.9 消融实验
- 6.10 失败案例分析

### 第七章 总结与展望

- 本章按照学校论文模板组织，不预设三级标题。

第一至四章与第七章的目录保持 v1.0 不变。第五章与第六章按 v2.0 的工作内容调整：第五章增加 5.3 实体消歧与事件去重实现，并把原来的"向量数据库实现"改为"向量索引构建实现"，与第 9.3 节的术语口径一致；第六章从"测试环境、功能测试、接口测试、性能测试、检索实验、问答实验、对比实验、消融实验、失败案例分析"扩展为 6.1 至 6.10，把测试环境与实验配置、评价指标定义与评分标准、抽取实验前置，使实验章节的写作顺序与第十二章的实验方案一一对应。

写作顺序建议采用"边做边写"，不要等系统彻底做完再开始写论文。前期写第一章至第三章，因为这些内容主要是研究背景、相关技术与需求分析，与开发进度无关；系统设计完成后写第四章；功能开发过程中写第五章，其中 5.3 节的实体消歧与事件去重、5.5 节的向量索引构建需要与实现进度同步记录；实验完成后写第六章，6.1 与 6.2 可以在实验开始前先写好，6.3 至 6.10 按实验完成顺序补充；最后写第七章。这样安排可以避免在最后阶段突然面对几十页空白内容。论文的排版、页数与具体格式要求以学校下发的模板为准（TBD）。

## 十六、进度安排

### 16.1 十二阶段总流程

```text
第1阶段  选题与项目规划
        ↓
第2阶段  文献调研与开题
        ↓
第3阶段  需求分析
        ↓
第4阶段  系统总体设计
        ↓
第5阶段  数据准备
        ↓
第6阶段  事件抽取与知识图谱
        ↓
第7阶段  RAG检索系统（普通 RAG）
        ↓
第8阶段  智能问答系统
        ↓
第9阶段  前后端系统集成
        ↓
第10阶段 系统测试 + 对比实验
        ↓
第11阶段 论文撰写与材料整理
        ↓
第12阶段 答辩准备
```

### 16.2 四个里程碑

| 里程碑 | 包含内容 | 达成标准 |
| --- | --- | --- |
| 里程碑一：能跑 | 项目架构 + 数据 + 普通 RAG | 能够问问题并返回答案 |
| 里程碑二：核心技术完成 | 事件抽取 + 知识图谱 + KG-RAG | AI 可以同时利用文本和图谱回答问题 |
| 里程碑三：软件完成 | Vue + FastAPI + MySQL + Neo4j + FAISS + LLM | 真正像一个完整的软件系统，页面、接口、三个数据存储与模型调用全部联通 |
| 里程碑四：论文完成 | 功能测试 + 性能测试 + RAG 对比 + 消融实验 + 论文 + PPT + Demo | 可以交毕业设计并进行答辩 |

### 16.3 时间分配

假设总周期约为 4～5 个月，可以按以下节奏安排：

| 阶段 | 时间 |
| --- | --- |
| 选题与规划 | 1 周 |
| 文献调研、开题 | 2 周 |
| 需求分析 | 1 周 |
| 系统设计 | 1～2 周 |
| 数据准备 | 2～3 周 |
| 事件抽取/知识图谱 | 3～4 周 |
| RAG检索系统（普通 RAG） | 2 周 |
| 智能问答系统 | 2～3 周 |
| 前后端开发 | 3～4 周 |
| 测试与实验 | 2～3 周 |
| 论文 | 持续进行 |
| PPT/答辩 | 最后 1～2 周 |

实际时间可以根据学校的节点压缩。压缩时优先保证里程碑一与里程碑二，即普通 RAG 与知识图谱增强问答必须跑通，因为它们决定了论文实验能否成立；功能层面的扩展可以在时间紧张时削减。需要注意的是，数据准备阶段与事件抽取/知识图谱阶段的工期中包含两项容易被低估的工作：一是 12.3 节 Dev 50～100 条（建议 60 条）与 Test 200 条抽取评测集的人工标注（合计约 250～300 条），二是实体消歧与事件去重的实现与核查。这两项工作不完成，RQ1 与图谱质量无法交代，因此不安排在时间压缩的范围内。

### 16.4 项目依赖顺序

```text
需求
 ↓
架构
 ↓
数据
 ↓
普通RAG
 ↓
知识图谱
 ↓
KG-RAG
 ↓
后端
 ↓
前端
 ↓
测试
 ↓
实验
 ↓
论文
 ↓
答辩
```

这条依赖顺序不能颠倒。不能先做前端，再做数据库，最后才考虑 AI 部分。尤其是普通 RAG 必须先跑通，再加知识图谱，否则最后出现问题的时候，无法判断是 RAG 的问题、知识图谱的问题、前端的问题，还是大模型的问题。

## 十七、数据"先小后大"推进策略

数据准备阶段不要一开始就设想"做 10000 家公司、100 万条新闻"，而应该按照"先小后大"的方式推进：

| 版本 | 规模 | 目的 |
| --- | --- | --- |
| 第一版 | 10 家公司 / 100 篇文本 | 跑通数据导入、清洗、事件抽取、图谱写入、向量检索与问答的完整流程 |
| 第二版 | 50 家公司 / 500 篇文本 | 检验流程在规模增加后的稳定性与抽取质量 |
| 第三版 | 100+ 家公司 / 1000+ 篇文本 | 用于最终对比实验与消融实验 |

这样安排的意义在于控制风险：第一版就是一个完整的小闭环，即使中途出现问题，也不会导致整个项目推倒重来，而是可以退回上一版继续工作。本科毕业设计的关键是数据能够稳定跑通、流程能够重复执行，而不是数据量越大越好。因此第三版的 100+ 家公司 / 1000+ 篇文本是努力目标而不是硬性验收指标：若进度紧张，可以在保证问题覆盖完整、标注完整与实验可复现的前提下停留在第二版规模，并在论文中如实说明实际数据规模、时间范围与数据截止时间。

数据来源限定为上市公司公告、监管机构公开信息、政策文件与正规财经新闻。数据准备的基本流程为：原始数据、数据采集、去重、清洗、格式统一、分词与切分、实体与事件抽取、实体消歧、事件去重、图谱写入与保存；每条文本至少保留标题、正文、发布时间、来源、URL、涉及公司与入库时间（ingest_time）等字段；数据集版本级属性 dataset_version 与 data_cutoff_time 记录在数据集元信息中（配置文件 ＋ 数据集说明文档），不存放在文档表中。与 v1.0 相比，v2.0 的流程补充了三点。

第一，在数据准备流程中增加"实体消歧"与"事件去重"两个环节。这两个环节放在抽取之后、图谱写入之前执行，实体按 stock_code 对齐并归并名称与别名，事件按类型、参与主体、时间窗口与触发词相似度判定并合并，合并后保留全部证据文档。这两个环节的产物（待消歧列表、合并日志）一并保存，用于失败案例分析。

第二，在事件抽取阶段同步构建 Dev 与 Test 两段式人工标注评测集，消除"用测试集调 Prompt"的数据泄漏。开发集 Dev 50～100 条（建议 60 条）用于抽取规则与 Prompt 的迭代调优，每迭代一轮在 Dev 上重新计算实体、事件、关系、时间的 Precision、Recall 与 F1，用数字判断规则或提示词的调整是否有效，而不是凭单条样例的主观感受决定是否继续调整；测试集 Test 200 条独立于 Dev，在规则与 Prompt 冻结后一次性评测、只测一次，Test 结果不得反馈用于再调优，若确需修改规则必须另行划定新的 Test 并在论文中说明。两集合不得重叠，均记录文本编号，标注总量约 250～300 条，标注工作与抽取工作在同一阶段完成，避免实验阶段才发现没有金标准。

第三，数据集版本级记录 data_cutoff_time 与 dataset_version：每篇文档在入库时记录 ingest_time（入库时间），而 data_cutoff_time 是数据集版本级属性，与 dataset_version 成对记录在数据集元信息中（配置文件 ＋ 数据集说明文档），不属于任何表，也不采用"取所有文档中的最晚时间"的推导口径。界面固定显示"数据截至：YYYY-MM-DD"，该属性同时写入检索与生成环节，用于按 event_time 解释"最新／最近／近期"类问题。

第一版不引入股吧。股吧的数据噪声太大，用户观点、猜测、传闻与事实混杂在一起，会显著增加知识图谱构建的难度，也会让事件抽取的时间与主体难以确认，同时股吧内容中大量存在主观判断与情绪表达，与本文件 5.3 节"公开事实与模型分析必须区分"的要求冲突。因此在本课题中把股吧写作"后续扩展的数据源"，不作为第一版的数据来源，也不进入实验数据集。

## 十八、风险与应对

| 风险 | 可能表现 | 应对措施 |
| --- | --- | --- |
| 财经数据获取与合规 | 数据源页面结构变化、访问受限、抓取频率被限制，数据使用范围不清 | 数据来源选取公开可访问的信息（上市公司公告、监管机构公开信息、政策文件与正规财经新闻），并遵循相应网站的访问规范、使用条款与学校毕业设计的相关要求；系统与论文仅用于毕业设计的研究与演示，保留原始来源、来源类型与发布时间等元数据，不提供任何投资建议；同时控制抓取频率，在论文中明确说明数据来源与使用范围 |
| 事件抽取准确率不足 | 实体漏抽、事件类型判断错误、时间抽取错误，导致图谱质量下降 | 按"先小后大"策略先在小规模数据上人工核查抽取结果，整理高频错误清单并迭代规则或提示词；使用 12.3 节的 Dev 集（50～100 条，建议 60 条）量化迭代效果，Test 集（200 条）在规则与 Prompt 冻结后一次性评测；事件属性中保留 confidence 字段；问答环节以原始文本证据为主、图谱关系为辅，降低错误传播 |
| 知识图谱未带来增益（RQ2 假设不成立） | KG-RAG 与普通 Vector RAG 的指标接近甚至更差 | 这本身是可报告的实验结论：按 12.8 的判定规则给出结论，分问题类型分析图谱在关系型、多跳问题、时序问题上的作用与在纯事实型（0 跳）问题上的局限；通过失败案例分析定位原因，按抽取错误、实体消歧错误、检索漏召回、时间判断错误、生成不忠实五类归因统计；论文结论表述为"验证知识图谱在什么条件下提供额外价值"，而不是预设它一定更强 |
| 时间不足导致范围蔓延 | 临近节点功能未完成、实验没有做完、论文内容空泛 | 按四个里程碑推进，优先保证里程碑一与里程碑二；测试集可从 120 题（核心 108 + 压力 12）压缩到最低 60 题（核心 54 + 压力 6），但标签体系与各类问题必须覆盖；抽取评测集标注（Dev 50～100 条＋Test 200 条，合计约 250～300 条）的工作量已计入 16.3 节的事件抽取/知识图谱阶段，不列入压缩范围；不增加多角色权限等超出边界的扩展；时间紧张时优先保证软件系统、论文与实验结果三项核心交付物 |
| 大模型调用成本与稳定性 | 接口限流或超时、费用超出预算、同一问题多次生成结果不一致 | 统一封装模型调用并设置超时与重试；对相同问题缓存结果；实验期间按 12.4 节固定配置并批量执行、保存原始输出，避免重复调用；准备备用模型接口；答辩演示前提前验证接口可用性，并准备离线材料以备现场网络异常 |
| 演示环境依赖 | 现场网络不可用、数据库或图数据库服务未启动、接口被限制访问 | 使用 Docker 统一运行环境并准备一键启动脚本；提前准备本地数据快照与预置问答样例；演示前完成完整彩排，确保"打开网站、输入问题、显示答案、显示证据、显示图谱"的流程可以在几分钟内稳定完成 |

## 十九、最终交付物清单

| 序号 | 交付物 | 内容说明 | 是否核心 |
| --- | --- | --- | --- |
| ① | 开题报告 | 选题背景、国内外研究现状、研究目标、研究内容、技术路线与进度计划 | 否 |
| ② | 软件系统 | 前端页面、后端接口、MySQL、Neo4j、FAISS 与模型调用的完整实现 | 是 |
| ③ | 数据集/知识图谱 | 清洗后的财经文本数据集，以及包含实体、事件、关系、时间与来源的事件知识图谱，并包含数据集版本级属性 dataset_version 与 data_cutoff_time | 否 |
| ④ | 实验结果 | 对比实验与消融实验的数据、指标统计、抽取实验结果与失败案例分析 | 是 |
| ⑤ | 毕业论文 | 按七章结构撰写，包含需求分析、系统设计、系统实现、测试与实验分析 | 是 |
| ⑥ | PPT | 答辩演示文稿，覆盖研究背景、研究目标、需求分析、系统架构、核心技术、系统展示、实验结果与总结 | 否 |
| ⑦ | 演示 Demo | 可现场运行的完整流程与备用演示材料 | 否 |

其中最核心的是：软件系统、论文、实验结果。其余交付物围绕这三项展开，任何时间安排上的取舍都以保证这三项完成为前提。

## 二十、下一阶段待办

本阶段（第 1 阶段）已完成：题目、研究目标、研究内容、项目边界、功能需求、非功能需求、系统总体架构、数据设计概要、时间维度、技术路线与实验方案均已确定并写入本文件。下一步进入第 2 阶段文献调研与开题。

### 20.1 第 2 阶段（文献调研与开题）调查方向

方向一，知识图谱：了解什么是知识图谱、金融知识图谱怎么做、事件知识图谱是什么，以及国内外有哪些相关研究。

方向二，RAG：了解普通 RAG、向量检索、混合检索、GraphRAG 与 RAG 评价方法。

方向三，金融问答：了解金融问答系统、财经文本处理、金融领域大语言模型与金融问答数据集。

方向四，软件系统：了解智能问答系统的系统架构、前后端分离方案与知识图谱系统设计。文献调研的成果按主题整理成文献笔记，标注来源、方法与结论，为开题报告与论文第二章提供素材；引用信息以实际检索到的文献为准，不得凭印象编造作者、期刊与年份。

### 20.2 第 2 阶段最终产物

第 2 阶段结束时应当形成：开题报告、国内外研究现状、研究目标、研究内容、技术路线与进度计划。同时需要能够回答一个基本问题：为什么我要做这个课题。该问题的回答要点可以直接取自本文件第三章与第四章。

### 20.3 答辩问题预案

以下问题需要在第 2 阶段开始同步准备，并在后续各阶段持续补充证据与数据：

- 为什么需要知识图谱？回答要点：多实体、多事件、多跳关系与时间约束问题仅靠文本相似度检索容易遗漏证据，图谱提供关系与路径。
- 为什么不用普通 RAG？回答要点：普通 RAG 作为 Baseline 2 与消融实验 A 组保留并参与对比，本课题要验证的是在其基础上图谱是否提供额外价值。
- 你的主要工作与特色是什么？回答要点：面向财经事件的事件中心化知识表示、融合实体关系与时间约束的知识图谱增强混合检索方法、面向财经事件多跳问答的证据追溯与系统化评测机制。
- 数据从哪里来？回答要点：上市公司公告、监管机构公开信息、政策文件与正规财经新闻，规模按第一版到第三版逐步扩展。
- 你的数据最近到什么时候？回答要点：系统不是实时行情数据库，data_cutoff_time 是数据集版本级属性，与 dataset_version 成对记录在数据集元信息中，界面固定显示"数据截至：YYYY-MM-DD"；"最新"取符合条件的、event_time 最大的事件（并列时全部返回），"最近"取 event_time ∈ [data_cutoff_time − 30 天, data_cutoff_time]，"近期"取 event_time ∈ [data_cutoff_time − 90 天, data_cutoff_time]，答案中注明判定区间与数据截止时间，论文中写明数据的时间范围。
- 事件怎么抽取？回答要点：先做实体识别，再做事件识别与关系抽取，最后抽取时间信息并与证据来源关联；抽取结果经过实体消歧与事件去重后写入图谱并保留置信度，抽取质量用 Dev（50～100 条，建议 60 条）调优、Test（200 条）在规则与 Prompt 冻结后一次性评测得到的实体、事件、关系、时间 Precision、Recall、F1 说明（标注总量约 250～300 条）。
- 知识图谱怎么构建？回答要点：6 类实体、8 种事件类型、9 条核心关系与六项事件核心属性，通过数据抽取、实体对齐（以 stock_code 为锚）与事件去重写入 Neo4j；除 EVIDENCED_BY 外其余 8 条核心语义关系均携带 source_doc_id、source_chunk_id 与 confidence 证据属性，事件参与主体由 PARTICIPATES_IN（Company／Person／Institution → Event）表达，政策事件与监管事件的发布／作出机构由 ISSUED_BY 表达。
- RAG 怎么实现？回答要点：文本切分与 Embedding 写入向量索引（FAISS），检索结果与图谱检索结果经过时间过滤、证据融合与排序后交给大模型生成答案。
- 如何评价系统？回答要点：检索指标、问答指标与系统指标三组指标，检索指标统一为 chunk 级主口径（Recall@K、Precision@K、MRR、Complete Evidence Recall@K），配合抽取实验、功能测试、接口测试与性能测试；评分标准实验前固定，不少于 20% 的样本间隔 3 天以上二次复评并报告前后评分一致率，即评分稳定性。
- 知识图谱有没有真正提升效果？回答要点：以 Baseline 2 与 Method 的对比结果（回答 RQ2／H1 的主要证据）以及消融实验 A 至 E 的数据作答，Baseline 1 仅作辅助观察；按 12.8 节的判定规则说明在哪些子集上满足判定条件，H3 由 C 与 D 的对比回答，E 组作为进一步优化实验单独报告。
- 如果 KG-RAG 效果不好怎么办？回答要点：按 12.7 节的指标与 12.8 节的判定规则如实报告结论，把"在什么条件下有效、在什么条件下无效"作为实验结论，失败案例按抽取错误、实体消歧错误、检索漏召回、时间判断错误、生成不忠实五类归因，并以可复现的数据与口径说明结论。

本文件经确认后作为执行基准使用。若题目、边界或实验方案发生调整，应更新本文件版本并在修订记录中登记；方向性调整还需要同步更新《项目总体方案（导师审阅版）》。
