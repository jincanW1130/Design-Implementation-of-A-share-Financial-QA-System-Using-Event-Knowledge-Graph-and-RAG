# 16-事件抽取与知识图谱（第六阶段）

> 性质：本文件是第 6 阶段（事件抽取与知识图谱）的**阶段交付文档**（《15-第6阶段任务书（事件抽取与知识图谱）》第4.1节 与 第4.3节 规定的产出），是论文 **第五章 5.2 事件抽取实现／5.3 实体消歧与事件去重实现／5.4 知识图谱构建实现** 的**实现说明与数据来源**，并为 第六章 6.3 抽取实验提供数据。它不是任务书、不是设计文档、也不是方法实验报告。
> 描述对象：`代码\抽取与图谱\` 的五个脚本在数据集 v2.1（105 家公司／709 篇文档／5018 个文本块）上的整链产物——按 doc_id 的抽取结果 `代码\抽取与图谱\_全量\v2.1\extracted.jsonl`、消歧映射与待消歧清单、去重合并日志与合并后事件集合，以及 `阶段06-事件抽取与知识图谱\图谱导出\v2.1\` 的四件导出物；外加抽取评测集 v2.1（Dev 60 ＋ Test 200）。
> 数字来源：正文每一个数字都实测自上述文件，逐处标注出处；参数一律取自 `代码\抽取与图谱\config.py` 的冻结值，本文件只引用、不重新论证。凡数字来自报告文件而非本文件现场重算的，正文写明来源文件名。
> 依据：《15-第6阶段任务书（事件抽取与知识图谱）》第2.2节 与 第八节；《10-系统总体设计（第四阶段）》第4.5.1节～第4.5.6节；《13-数据准备（第五阶段）》第6.8节 与 第9.1节；《02-项目执行总控文档》（**v2.8**；版本链：编制时为 v2.7 → 第 6 阶段收口时按《15》第2.4节（二）的注记要求登记为 v2.8，抽取侧三项参数属上游数据生产参数、不并入 第12.4节 的七项，七项的计数与取值未变）第9.2节、第10.1节、第10.3节、第12.3节、第12.4节、第12.7节。
> 编制日期：2026-09-26。

| 项目 | 内容 |
| --- | --- |
| 阶段 | 第 6 阶段 事件抽取与知识图谱 |
| 对应论文章节 | 第五章 5.2 事件抽取实现；5.3 实体消歧与事件去重实现；5.4 知识图谱构建实现（为 第六章 6.3 抽取实验提供数据） |
| 本阶段性质 | **实现说明 ＋ 数据来源**：说清四个环节怎么实现、产出是什么、每个数字怎么来的 |
| 上游依据 | 数据集 v2.1（《13》第6.8节）；本体与口径冻结在《10》第4.5节；输入与验收口径在《15》第三节 与 第八节 |
| 下游去向 | 第 7 阶段（向量检索与图谱查询）、第 10 阶段（抽取评测集与实验） |
| 核心交付判断 | 整链可重放（导出物逐字节一致、重放零模型调用）、导出物可复核、评测集可标注 |
| 状态 | 本阶段产出（T1～T9 执行完毕，文档、导出物与评测集同步定稿） |
| 日期 | 2026-09-26 |

**一页速查**

| 维度 | 实测值 |
| --- | --- |
| 输入 | v2.1：105 家公司／709 篇文档／5018 个文本块／5018 条向量；data_cutoff_time 2026-09-25；语料窗口 2026-06-17～2026-09-25 |
| 抽取配置 | 模型请求名 `deepseek-v4-flash`（端点解析名 `deepseek-flash`）／Prompt 版本 `stage6-extract-v1.1`／temperature 0 |
| 抽取结果 | 实体 3753／事件 1649／语义关系 3066／EVIDENCED_BY 1649；拒绝 81 条；警告 531 条 |
| 实体消歧 | 3753 条提及 → 2618 条已消歧（合并为 1179 个实体节点）／1135 条待消歧；公司提及消歧率 753／1888 |
| 事件去重 | 1649 → 1623（合并 24 组／27 对，合并掉 26 条） |
| 图谱导出物 | 节点 3507／边 3733（含 EVIDENCED_BY 1644）；客户关系 CUSTOMER_OF 本版 0 条（见 第8.2节） |
| 抽取评测集 | Dev 60 ＋ Test 200 ＝ 260 条（单位：文本块）；chunk 交集 0、文档交集 0；分层容差 3.0 pp |
| 可重放性 | 导出物逐字节一致（仅 generated_at 与缓存指纹变化）；重放模型调用 0 次；清单哈希 `9e6ae89eaf942b53` |

## 一、抽取口径与配置

### 1.1 抽取四要素（固化值）

| 要素 | 取值 | 说明 |
| --- | --- | --- |
| 模型（请求名） | `deepseek-v4-flash` | `config.LLM["model_default"]`；709 篇全部按该名抽取请求 |
| 模型（解析名） | `deepseek-flash` | `config.LLM["model_pinned"]`；`model_pinning_state` 为 ok 的 709／709 |
| 模型版本 | 端点不提供版本号，如实记 null | `config.LLM["model_version"]` 为 null 并附说明：`/models` 只返回 id 与 owned_by，不返回 revision，因此**抽取可重放性由缓存承担**（同一 doc_id 的原始返回逐字节留档），不依赖「同一次请求必得同一段文本」这一假设 |
| Prompt 版本 | `stage6-extract-v1.1` | `config.LLM["prompt_version"]`；模板摘要 sha256 为 `13b4522b7d4a17…`（SYSTEM_PROMPT ＋ 分隔符 ＋ USER_TEMPLATE 计算，逐篇写入缓存记录 `prompt_template_sha256`） |
| temperature | 0 | `config.LLM["temperature"]`；抽取时固定 0，重放对得上 |

### 1.2 其余固化参数

| 参数 | 取值 | 出处 |
| --- | --- | --- |
| 接口形态 | OpenAI 兼容的对话补全端点 | `config.LLM["provider"]` |
| 输出格式 | json_object | `config.LLM["response_format"]` |
| 单次输出上限 | max_tokens 8192 | `config.LLM["max_tokens"]`，压缩重试同值 |
| 超时 | 180.0 秒 | `config.LLM["timeout_seconds"]` |
| 请求节奏 | 最小间隔 1.0 秒；最多重试 4 次；连接错误／超时／429／5xx 重试，4xx（除 429）不重试 | `config.PACING` |
| 单篇抽取上限 | 实体 12／事件 4／关系 16 | `config.EXTRACT_LIMITS` |
| 压缩重试 | 触发原因是 finish_reason 为 length；最多 2 次；压缩后上限为实体 8／事件 2／关系 6、引文 60 字符 | `config.LLM["fallback"]` |
| 缓存与重放 | 缓存命中不调用模型；重放时模型调用上限 0，超限即非零退出 | `config.RUN_ALL["max_api_calls_on_replay"]` |
| 复跑日志口径 | 首跑 `运行日志_首跑.txt`、复跑 `运行日志_复跑.txt` | `config.RUN_ALL` |

### 1.3 密钥与可重放性

- 密钥只经环境变量注入：`config.py` 只写读取方式（`config.LLM["api_key_env"]` 取值为 `LLM_API_KEY`），不写取值；本文件、导出物与全部交付物都不含密钥取值，仓库内也没有任何凭据。缓存命中时**一次模型调用都不发生**，复跑因此不需要密钥（缺席即硬失败，不降级、不编造）。
- 复跑读数（数字来源：`阶段05-数据准备\数据集\_抽取缓存\v2.1\图谱管线\运行日志_复跑.txt`）：参数 `--force=True --allow-api-calls=False`；四环退出码全为 0，耗时 extract 1.200 s／disambiguate 0.259 s／dedup_events 8.369 s／write_graph 1.263 s；709 篇全部缓存命中、模型调用 0 次；产物清单 713 个文件、清单哈希 `9e6ae89eaf942b53`；输入抽取结果指纹 `4cd9bcceb40e3c75…`（709 篇）。
- 逐字节复现的比对依据写在 `manifest.sha256`；`graph_stats.json` 每次运行的 `generated_at` 不同，比对时排除该字段与缓存指纹字段（`config.GRAPH_PIPELINE["excluded_from_byte_compare"]`）。

### 1.4 与《02》第12.4节「实验配置固定表」的关系（边界说明）

- 《02》第12.4节 的七项是**实验配置固定表**，其中答案侧的大语言模型与版本、Top-K、Context Token Budget 仍为 TBD，归属第 8 阶段与预实验固化；本阶段不涉及、不填值、也不替它们选型，一律保持原样。
- 抽取所用的模型与 Prompt 版本属**上游数据生产参数**（它们决定图谱里有什么、不决定答案怎么生成），不是那七项；它们固化在 `config.LLM` 并登记在本章 1.1。改动其中任何一项都必须同步升 Prompt 版本号并按 T3→T4→T5→T6 重跑。
- 本文件据此**不描述答案生成侧的实现与配置**：那是第 8 阶段的内容。本文件只说明「图谱里有什么、怎么来的」。

### 1.5 覆盖性重算（T1：写入本文件并作为标注配比的唯一依据）

- 表 15-C 重算：`company_list ≥ 2` 命中 66 篇（9.3%）／`subject_companies ≥ 2` 命中 9 篇（1.3%），与《15》第2.2节 表 15-C 声明逐项一致。
- 表 15-D 重算（**关键词命中篇数，是代理指标，只用于判断「有没有证据」、不是抽取结果**）：重大合同 **100**（公告标题级）／产品 **102**（公告标题级）／SUPPLIES **32**（正文级）／CUSTOMER_OF **88**（正文级）／COMPETES_WITH **9**（正文级），与《15》第2.2节 表 15-D 声明逐项一致。
- 表 15-E 重算：公告 4042 块／政策文件 284 块／财经新闻 563 块／监管公开信息 129 块，合计 5018 块；逐月 2026-06 1226／2026-07 937／2026-08 1620／2026-09 1235，与《15》第2.2节 表 15-E 声明一致（该表逐月值经核对无需更正）。
- 数字来源：`阶段05-数据准备\数据集\抽取评测集\v2.1\分层统计.json` 的 `table_15C_15D_recompute` 与 `table_15E_recompute` 两段，由 `代码\抽取与图谱\sample_eval_set.py` 重算；本文件不重复实现该重算逻辑，只引用其结果。
- 覆盖性重算是**输入侧的检查**（语料里有没有这类信号），不是抽取结果的评价；抽取结果的读数在第三章。

### 1.6 抽取规模、成本与异常读数

| 读数 | 实测值 | 出处 |
| --- | --- | --- |
| 文档 | 处理 709／成功 709／失败 0 | `report_stats.json` |
| 首跑调用 | 737 次（缓存命中 14 次），新取 694 篇 | 同上 `run_history.first_full_pass` |
| 重试 | 压缩重试 38 篇（尝试 1 次者 671 篇、2 次者 38 篇），重试上限 2 次 | 同上 `attempts_histogram` |
| 截断 | finish_reason 为 length 的 0 篇，全部为 stop | 同上 `docs_final_finish_reason_length` |
| token | 首跑 prompt 2,091,704 ＋ completion 3,056,728 ＝ 5,148,432；其中推理 token 2,521,261 | 同上 `usage_total_sum` 与 `reasoning_tokens_sum` |
| 墙钟 | 首跑 11789.07 秒（约 3 小时 16.5 分）；12 篇试跑外推全量的预估为 5,498,886 token／约 3.47 小时 | 同上 `run_history` 与 `t1_extrapolation` |
| 上游质量 | 一次通过 708 篇、首轮失败后重试通过 1 篇、最终失败 0 篇 | 同上 `documents_failed` 等 |
| 零覆盖 | 无「一次都没抽到的实体类型／事件类型／关系」 | 同上 `zero_coverage`（各键均为空） |

## 二、Prompt 与规则设计

### 2.1 提示词结构

- 提示词＝SYSTEM_PROMPT（系统提示）＋ USER_TEMPLATE（用户模板），绑定 `LLM["prompt_version"]` 的 `stage6-extract-v1.1`；改其中任何一个字都必须同步升版本号。模板摘要 sha256 为 `13b4522b7d4a17…`，逐篇写入缓存记录。
- 系统提示钉死四件事：只输出一个 JSON 对象（不输出解释、不输出 Markdown 代码块、不输出多余字段）；字段名与枚举之外一律不输出；每条实体／事件／关系都必须给出**正文逐字引用** `quote`；实体 `name` 必须是正文里连续出现的写法（正文只写简称就写简称，不补全成公司全称、不改标点）。
- 用户模板分四段：【文档元信息】（doc_id／category／source／publish_time／title）／【正文】（整篇文档正文）／【只输出下面这一个 JSON 对象，字段名照抄】（entities／events／relations 三个数组及其字段）／【规则】1～7 条。
- 枚举与上限一律以占位符从 config 注入模板（实体类型、事件类型、模型侧关系、role 取值、四个上限），脚本内不重复写字面量；这样「枚举改了而模板没改」的静默漂移在结构上不可能发生。

### 2.2 七条输出规则（逐条）

1. `entities` 只输出 5 类（Company／Person／Industry／Institution／Policy）；事件由 `events` 数组给出，不在 `entities` 里重复；EVIDENCED_BY 不由模型输出，由程序按 doc_id 生成；同一实体只输出一次。
2. `events` 按正文出现顺序编号 E1、E2…，关系里用 E1 这样的引用指事件；`event_type` 只能取冻结的 8 种。
3. `relations` 只输出 8 类，端点类型必须符合 schema；ISSUED_BY 只用于「政策」或「监管」事件、终点必须是 Institution，且同一机构既是发布／作出方又参与该事件时只写 ISSUED_BY、不再写 PARTICIPATES_IN。
4. `quote` 必须来自正文、逐字连续、长度 12～200 字；引用标题或改写过的文字会被程序判为无法定位并**整条丢弃**。
5. `event_time` 只在正文写明到「日」时填写；只写到月、或需要靠推断的，一律写 null；`valid_from`／`valid_to` 只在正文给出明确日期时填写。
6. 没有把握的条目一律不输出——**宁可少抽，不可编造**。
7. 输出紧凑：事件描述限一句话，不复述提示词、不输出解释或推理过程。

### 2.3 证据定位由代码完成，不由模型给编号

- 模型只给逐字引用，代码做「折空白 ＋ 精确子串」匹配；比对键**去掉全部空白**（含全角空格），其余字符照原样。理由可查：来源 PDF 存在词内空格（doc_id 1018 的正文写作「公 司将」），若把空白折成单空格，合法引用会被误判为无法定位（`config.EVIDENCE["normalize"]`）。
- 定位顺序：在该文档的文本块序列里按 chunk_index 升序查；命中多块时取 **chunk_index 最小**的一块，并累计 `quote_match_count`。
- 引文长度门 12～200 字；标题永不作为证据（`title_is_not_evidence` 为 true）；跨块边界的引文一律拒绝。
- 抽取以**文档**为上下文、以**文本块**为证据单位：一篇文档一次调用，事件不会被块边界切断；每条事实的 `source_chunk_id` 由代码定位得到，模型不给编号。

### 2.4 本地校验：拒绝与警告（不静默改写）

拒绝 81 条（分布在 46 篇文档上，数字来源 `report_stats.json` 的 `rejection` 段）：

| 拒绝原因 | 条数 | 含义 |
| --- | --- | --- |
| relation_domain_range_illegal | 21 | 关系端点类型不符合 schema 的 domain／range |
| quote_not_in_document | 13 | 引文在文档内定位不到（改写、抄写走样或跨块拼接） |
| issued_by_event_type_illegal | 12 | ISSUED_BY 落在「政策」「监管」以外的事件上 |
| event_ref_unresolved | 9 | 关系里的事件引用（如 E3）在 events 数组里不存在 |
| endpoint_type_mismatch | 8 | head／tail 的名称与声明的端点类型不一致 |
| entity_ref_unresolved | 8 | 关系里的名称不在本页 entities 数组里 |
| quote_spans_chunk_boundary | 7 | 引文跨文本块边界，无法指向唯一文本块 |
| relation_not_allowed | 3 | 关系名不在 8 类之内 |
| 合计 | 81 | 拒绝只发生在本地校验，被拒条目既不入图谱也不改写 |

警告 531 条（不拒绝，只登记，数字来源同上的 `warnings` 段）：

| 警告 | 条数 | 含义 |
| --- | --- | --- |
| quote_ambiguous | 415 | 引文在文档里出现多次，取 chunk_index 最小者并累计 quote_match_count |
| event_time_unverified | 100 | 事件写了时间但正文无法核实到日（「近日」「本月」这类表述），事件里记 null |
| event_without_participant | 16 | 事件没有任何参与主体（个别政策／监管事件只由发布机构给出） |
| 合计 | 531 | — |

### 2.5 压缩重试

- 38 篇触发过一次压缩重试：首轮返回被输出上限截断（finish_reason 为 length）时，第二次调用追加压缩指令，并把上限收到实体 8／事件 2／关系 6、引文 60 字以内，要求「只保留最重要的」。
- 两次原始返回全部进缓存（按 doc_id 一篇一文件），因此重试不改可重放性；重试后 finish_reason 全部为 stop，最终失败 0 篇。
- 上限与触发原因都固化在 `config.LLM["fallback"]`，不是运行时拍脑袋定的。

## 三、抽取结果统计

### 3.1 总览

| 项 | 条数 | 出处 |
| --- | --- | --- |
| 文档 | 709 | `extracted.jsonl` 709 行，`report_stats.documents_processed` |
| 实体提及 | 3753 | `report_stats.totals.entities` |
| 事件 | 1649 | `report_stats.totals.events` |
| 语义关系（除 EVIDENCED_BY 外的 8 条之和） | 3066 | `report_stats.totals.semantic_relations` |
| EVIDENCED_BY | 1649 | 一条事件一条证据文档边，程序生成 |
| 证据项合计 | 8468 | `verify_summary.evidence_items_checked`（＝3753＋1649＋3066） |
| 拒绝／警告 | 81／531 | `report_stats.totals` |
| 证据属性校验 | 通过 3066／3066，坏项 0 | `verify_summary.evidence_attrs_ok` 与 `evidence_items_bad` |
| 复现性自检 | `reproducibility_identical` 为 true，枚举违规 0 | `verify_summary` |

### 3.2 实体类型分布（6 类）

| 实体类型 | 提及条数 | 占比 |
| --- | --- | --- |
| Company | 1888 | 50.3% |
| Institution | 914 | 24.4% |
| Person | 573 | 15.3% |
| Policy | 257 | 6.8% |
| Industry | 121 | 3.2% |
| 合计 | 3753 | 100.0% |

读法：`Event` 是第 6 类实体类型（《10》第4.5.1节），但事件由 `events` 数组给出、不重复计在上述提及里；`Document` 是证据文档标签，不是第 7 类实体。实体名一律用语料写法、不做归一化——归一化只在消歧的匹配口径里当比较键（见 4.1），不改抽取结果。

### 3.3 事件类型分布（8 种）

| 事件类型 | 抽取条数 | 占比 |
| --- | --- | --- |
| 业绩 | 227 | 13.8% |
| 监管 | 160 | 9.7% |
| 股权 | 363 | 22.0% |
| 投资并购 | 46 | 2.8% |
| 重大合同 | 165 | 10.0% |
| 产品 | 172 | 10.4% |
| 政策 | 93 | 5.6% |
| 重大经营 | 423 | 25.7% |
| 合计 | 1649 | 100.0% |

读法：这是**实测分布，不是设计配额**，由语料自身的题材构成决定；不得读成按类均衡，也不得据此假定每类证据量相当（薄类见 第8.6节 与 第8.8节）。

### 3.4 核心关系分布（9 条，导出物口径）

| 关系 | 导出物条数 | domain → range | 证据属性 |
| --- | --- | --- | --- |
| BELONGS_TO | 15 | Company → Industry | 三项 ＋ valid_from／valid_to |
| SUPPLIES | 3 | Company → Company | 三项 |
| CUSTOMER_OF | 0 条（抽取到 10 条，端点全部未消歧，未写入图谱；逐条清单见同目录的待人工确认清单） | Company → Company | 三项 |
| COMPETES_WITH | 1 | Company → Company | 三项 |
| HAS_EXECUTIVE | 159 | Company → Person | 三项 |
| PARTICIPATES_IN | 1592 | Company／Person／Institution → Event | 三项 ＋ role |
| ISSUED_BY | 172 | Event → Institution（只出现在政策／监管事件上） | 三项 |
| RELATED_TO | 147 | Event → Policy | 三项 |
| EVIDENCED_BY | 1644 | Event → Document | 不带三项（终点即证据文档本身） |

读法：条数取自 `graph_stats.json` 的 `counts.edges_by_relation`，是**导出物**口径（已排除端点未消歧的边，见 6.2）；抽取层口径见 3.6。除 EVIDENCED_BY 外的 8 条关系的三项证据属性是 `source_doc_id`／`source_chunk_id`／`confidence`（《10》第4.5.2节 与 第4.5.5节）；role 只出现在 PARTICIPATES_IN 上。

### 3.5 role 取值分布（PARTICIPATES_IN 的 2343 条）

| role | 条数 | 占比 |
| --- | --- | --- |
| 主体 | 1570 | 67.0% |
| 涉及方 | 472 | 20.1% |
| 合作方 | 205 | 8.8% |
| 监管方 | 81 | 3.5% |
| 受影响方 | 15 | 0.6% |
| 合计 | 2343 | 100.0% |

读法：role 的枚举取值由《10》第4.5.2节 冻结；「监管方」只用于参与事件但不是发布／作出方的机构（发布／作出方一律写 ISSUED_BY，见 6.2 的 issuer_not_also_participant 自检）。

### 3.6 抽取层口径（含未写入图谱的条目）

- 抽取层关系分布：PARTICIPATES_IN 2343／EVIDENCED_BY 1649／HAS_EXECUTIVE 307／ISSUED_BY 172／RELATED_TO 147／BELONGS_TO 42／SUPPLIES 38／CUSTOMER_OF 10／COMPETES_WITH 7；其中语义关系合计 3066（数字来源 `report_stats.relation_types`）。
- event_time 为空 989／1649（59.97%），非空 660 条；导出层空值同为 989，分母变为 1623（60.9%）——去重的时间窗条件要求两个日期都可解析，因此被合并掉的 26 条都带日期，空值条数在去重前后一致（`report_stats.event_time_null`、`merge_summary.counts.events_without_event_time`、`graph_stats.event_core_attributes.event_time_null_count` 三处同值）。
- 空值按事件类型分布（抽取层，分母为该类型抽取条数）：重大经营 210／423、业绩 170／227、产品 161／172、监管 126／160、重大合同 114／165、股权 113／363、政策 69／93、投资并购 26／46。
- BELONGS_TO 抽取到 42 条，其中带语料给出有效期的 **0 条**（`report_stats.belongs_to_with_valid_range`）；导出物 15 条的有效期也全为空（见 第8.3节）。
- 无参与主体的事件 388 条（`merge_summary.counts.events_without_participants`）：抽到了事件但正文没写出公司主体，按口径如实保留、不编主体。

### 3.7 从抽取层到导出层的三条对账恒等式

1. 实体侧：3753 条提及 − 1135 条待消歧 ＝ 2618 条已消歧，合并为 1179 个实体节点（Company 104 ＋ Industry 97 ＋ Institution 371 ＋ Person 454 ＋ Policy 153）；再加 705 个 Document 节点与 1623 个 Event 节点，等于节点总数 3507。
2. 事件侧：1649 − 26（合并掉）＝ 1623 个 Event 节点。
3. 关系侧：语义关系 3066 − 973（端点未消歧而跳过）− 4（重复边去重）＝ 2089 条语义边；再加 1644 条 EVIDENCED_BY，等于边总数 3733。

三条恒等式两边逐项相等，是本文件数字可追溯的凭据；核对命令见 6.5。未消歧提及不产生节点（`include_unresolved_entities` 为 false），这是 3507 而不是 4642 的原因。

## 四、实体消歧实现

### 4.1 三条匹配规则（《10》第4.5.4节 的落地）

- **比较键**：先归一化——去掉全部空白、剥掉最外层包裹字符（书名号、引号、括号等，`strip_outer_chars`），再比较。归一化**只作匹配键，不改抽取结果里的实体名**。
- **R1_exact_name_or_code**：书写面等于别名表里某公司的归一化全称／简称／注册全称，或等于 6 位股票代码。
- **R2_full_name_contains_short_name**：书写面包含某公司的归一化书写面，且**残余部分不含区分性标记**（控股／投资／实业／资本／集团控股 共 5 个，`distinct_entity_markers`）。残余含标记时记 `distinct_entity_marker` 并拦住、不合并——这是「母公司 ≠ 子公司」的最小代价写法。简称最短 2 字（`min_short_name_chars`）。
- **非公司标签**（Industry／Institution／Person／Policy）按「同类型 ＋ 同归一化名」合并（`non_company_rule`）。
- **多解**：命中多个公司时记 `ambiguous_alias`（本批 0 条）。
- **匹配失败**：留在待消歧列表，策略为 `pending_confirmation_exclude_from_graph`——按《10》第4.5.4节 的原话「匹配失败进入待消歧列表，人工确认后再写入图谱」，本阶段不猜、不合并、也不写图。

### 4.2 别名表的两个只读来源

- 公司名单来自第 5 阶段冻结配置 `代码\数据准备\config.py` 的 `COMPANIES`（105 家）。**只读**：不改名单、不改简称、不在本阶段扩名额。
- 注册全称补充来自 `代码\抽取与图谱\company_registered_names.py`（记录 schema `stage6-company-registered-names-1.0`，来源字段 `cninfo_company_intro`，105 家逐家带请求端点、字段名、HTTP 状态与响应摘要）。
- 补充名只在**语料佐证计数 ≥ 2** 时启用（登记模块与配置同名的下限常量）；佐证不足的登记名不参与匹配，避免把接口返回的写法直接当成语料事实。

### 4.3 消歧实测结果

| 标签 | 提及条数 | 已消歧 | 待消歧 | 合并出的节点数 |
| --- | --- | --- | --- | --- |
| Company | 1888 | 753 | 1135 | 104 |
| Industry | 121 | 121 | 0 | 97 |
| Institution | 914 | 914 | 0 | 371 |
| Person | 573 | 573 | 0 | 454 |
| Policy | 257 | 257 | 0 | 153 |
| 合计 | 3753 | 2618 | 1135 | 1179 |

- 公司提及消歧率 753／1888 ＝ **39.9%**。
- 产出公司身份键 104 个；其中 73 家把多种书写面合并到同一身份（`company_identities_with_multiple_surfaces`）。
- 注册名来源计数：105 家全部来自 `cninfo_company_intro`，缺失 0 家（`company_registered_name_sources` 与 `company_registered_names_missing`）。
- 数字来源：`阶段05-数据准备\数据集\_抽取缓存\v2.1\图谱管线\消歧\disambiguation.json`（及其别名表 `alias_table.json`）。

### 4.4 待消歧清单的构成（1135 条）

| 原因 | 条数 | 判定依据 |
| --- | --- | --- |
| no_alias_match | 1127 | 与 105 家名单的全部书写面（6 位代码／简称／注册全称）既不相等、也不互相包含 |
| distinct_entity_marker | 8 | 书写面包含某家公司的书写面，但残余带 5 个区分性标记之一 |
| 合计 | 1135 | 记录落在 `消歧\unresolved.jsonl`，逐条带 doc_id、entity_id、归一化名、原因与备注 |

- 8 条 `distinct_entity_marker` 的书写面举例：中国石化盛骏国际投资有限公司（含「中国石化」）、宁德时代新能源产业投资有限公司（含「宁德时代」）、紫金矿业股权投资管理（厦门）有限公司（含「紫金矿业」）等——都是**子公司写法**，合并会把子公司并进母公司，故如实拦住。
- 1127 条 `no_alias_match` 经逐条复核，与 105 家名单的书写面毫无包含关系：构成是同行公司、境外主体与非上市公司（名单之外的主体）。
- 本文件不把待消歧当错误：它是冻结口径的执行结果，数量与「名单只有 105 家、语料涉及上千个机构主体」这一事实相符（见 第8.4节）。

### 4.5 必须说明的边界：603501

- 105 家中 104 家产出了身份键，**603501 没有**：登记全称 `豪威集成电路(集团)股份有限公司`（半角括号）与语料里的 7 处提及都不是同一写法（6 处全角括号、1 处繁体写法），语料佐证计数为 0，补充名未启用，因此该公司的提及全部留在待消歧。
- 该登记行另记录了接口返回的证券简称「豪威集团」，与本项目冻结简称「韦尔股份」不同；这是**登记源与冻结配置之间的口径差**，本阶段只登记、不改名单、不改简称。
- 处理动作与逐条清单见 `阶段06-事件抽取与知识图谱\图谱导出\v2.1\待人工确认清单.md` 的附录。

## 五、事件去重实现

### 5.1 四项条件（全部满足才合并）

1. **event_type 相同**。
2. **参与主体集合相等**：PARTICIPATES_IN 的 head 集合 ∪ ISSUED_BY 的 tail 集合，按**集合判等**（不看顺序、不看重复）。
3. **时间窗 ≤ 7 天**（`time_window_days`），且**两个日期都必须可解析**——任一为空，该条件不成立（`time_window_requires_both_dates`）。
4. **触发相似度**：`event_name` 的字符二元组 Jaccard ≥ 0.5（`similarity_min`），相似度保留 3 位小数（`similarity_round`）。T3 不产出独立的 trigger 字段，故用 event_name 作触发面（`similarity_field`）。

- **连接方式**：并查集单链（`union_find_single_linkage`）——两两满足即同组，组内不要求两两互满（传递闭包）。
- **代表元**：最早 publish_time，其次 doc_id，再次 local_event_id（`representative_rule`）。
- **合并后取值**：event_time 取成员中最早的非空值；confidence 取成员最大值（保留 3 位小数）；证据为成员证据文档的**并集**（`evidence_rule`）。
- **自检**：把 doc_id 整体平移 900000 造「同文异号」夹具，四项条件应不成立；夹具只用于自检，不进导出物、不进合并日志，只写 `dedup_self_test.json`。

### 5.2 合并日志读数

| 项 | 实测值 | 出处 |
| --- | --- | --- |
| 去重前事件 | 1649 | `report_stats.totals.events` |
| 去重后事件 | 1623 | `merge_summary.counts.events_after` |
| 合并组数 | 24 | `merged_groups` |
| 合并对 | 27 | `merged_pairs` |
| 被合并掉的事件 | 26 | `events_merged_away` |
| 最大组规模 | 3 | `max_group_size` |
| 单组最大证据文档数 | 3 | `max_evidence_docs_in_group` |
| 候选对 | 226876 | `candidate_pairs` |
| 首个不满足条件的分档 | 参与主体 225307／时间窗 1451／触发相似度 91／全部满足 27 | `first_failed_condition_histogram` |
| 合并阶段跳过的关系 | 0 | `relations_skipped` |

读法：候选对 226876 ＝ 225307＋1451＋91＋27（每一对只计入**第一个**不满足的条件）。首关（事件类型与参与主体）即淘汰 225307 对，占 99.3%；再用时间窗（1451 对）与触发相似度（91 对）收口；剩下 27 对全部满足，聚成 24 组。

### 5.3 跨文档合并的实例（证明去重确实跨篇工作）

| 合并组编号 | 事件名 | 证据文档 |
| --- | --- | --- |
| MEVT-0036 | 董事长郑宗强提议回购公司股份 | 1018／1103 |
| MEVT-0043 | 宁德时代获中国银行间市场交易商协会接受科技创新债券注册 | 1021／1163 |
| MEVT-0123 | 潍柴动力召开2026年第二次临时股东会并审议通过全部议案 | 1056／1057 |
| MEVT-0216 | 公司董事会审议通过回购股份议案 | 1100／1207／1279（组规模 3） |
| MEVT-0265 | 中国铁建董事会审议通过2026年半年报及摘要议案 | 1129／2003 |
| MEVT-1525 | 农业农村部印发《全国渔业发展「十五五」规划》 | 3009／3012 |

- 24 组里 **20 组跨文档**（同一事件被两篇及以上文档报道，例如同一份回购方案的独立公告与法律意见书）、**4 组同文档**（同一篇文档里同一事件被重复抽出）：同文档的 4 组是 MEVT-0001、MEVT-0130、MEVT-0539、MEVT-0914。
- 跨文档合并的意义：它把「同一事实的多篇报道」收敛成一个事件节点，下游按事件查证据时不会看到同一事实的多个副本。
- 完整 24 组见 `merge_summary.json` 的 `merged_groups_detail` 与 `merge_log.jsonl`。

### 5.4 去重的边界（如实登记）

- 去重只作用于**事件**：不合并实体、不合并关系（实体的合并靠消歧，见第四章）。
- 合并是保守的：1649 条里只合并掉 26 条（1.6%）。主要原因是条件 3 要求两个日期都可解析，而 event_time 为空的 989 条之间**永远不合并**（宁可漏合、不可错合）；触发相似度门 0.5 也会放过表述差异较大的同一事件。
- 合并组的证据是并集，**不丢证据文档**；同一合并组有多篇证据文档时，导出物会出现多条 EVIDENCED_BY（见 6.2）。
- **两套编号不得混用**：去重阶段的合并组编号是 `MEVT-xxxx`，导出物的事件编号是 `EVT-xxxx`，二者由不同环节按各自的确定性排序分配，序号**不对应**（例如 MEVT-0036 是回购事件，而 EVT-0036 是另一条利润分配事件）。跨文档引用事件时必须写明编号属于哪一套。

## 六、图谱写入与导出

### 6.1 节点与编号

- 标签共 7 种：6 类实体（Company／Person／Industry／Institution／Policy）＋ Event ＋ Document。**Document 是证据文档标签，不是第 7 类实体**（《10》第4.5.1节）。
- 编号规则（`config.GRAPH` 的 `id_rule`）：Company 用股票代码；Document 用 doc_id；Person／Institution／Policy／Industry 按（类型, 归一化名称）排序一次性分配 PER／INST／POL／IND 编号；Event 按确定性排序分配 EVT 编号。编号宽度 4 位（`id_width`），形如 `EVT-0001`、`PER-0001`。编号不依赖任何数据库自增，重放可复现。
- 节点 **3507**：Company 104／Event 1623／Document 705／Person 454／Institution 371／Policy 153／Industry 97（`graph_stats.counts.nodes_by_label`）。
- 4 篇文档（doc_id 1133／2033／2064／2084）没有抽到任何事件，因而不产生 Document 节点（Document 节点由 EVIDENCED_BY 触发）：705 ＝ 709 − 4。
- 节点表 33 列（`node_columns`）：编号、标签、名称，以及各类型的专有属性与 Document 的文档元信息（标题、来源、url、发布时间、类目）。

### 6.2 边与证据属性

- 边 **3733**：EVIDENCED_BY 1644／PARTICIPATES_IN 1592／ISSUED_BY 172／HAS_EXECUTIVE 159／RELATED_TO 147／BELONGS_TO 15／SUPPLIES 3／COMPETES_WITH 1／CUSTOMER_OF 0。
- EVIDENCED_BY 的构成：1603 个事件各 1 条、19 个事件各 2 条、1 个事件 3 条，合计 1603＋38＋3 ＝ 1644。多出来的 21 条来自合并组的多篇证据文档，不是重复行。
- 证据属性口径照《10》第4.5.2节 与 第4.5.5节：除 EVIDENCED_BY 外的 8 条关系共 2089 条边，带齐 `source_doc_id`／`source_chunk_id`／`confidence`，缺失 0 条；BELONGS_TO 另带 `valid_from`／`valid_to`（15 条全为空，见 第8.3节）；EVIDENCED_BY 不带这三项，其终点 Document 节点本身就是证据。
- 跳过 **973 条边**（`unresolved.relations_skipped`），原因全部是端点未消歧：按关系分 BELONGS_TO 27／HAS_EXECUTIVE 148／PARTICIPATES_IN 747／SUPPLIES 35／CUSTOMER_OF 10／COMPETES_WITH 6。这不是丢弃，是**冻结口径的正确结果**（《10》第4.5.4节）。
- 边去重：同一（关系, 起点, 终点, 证据块）重复的行去掉 4 条（`edge_dedup.duplicate_rows_removed`）。
- 孤立节点 **551**：PER 240／INST 164／IND 86／POL 57／Company 4（股票代码 000661／002022／002090／002541）——抽到的实体在语料里没有给出任何关系，如实保留、不删；孤立节点在下游检索里不可达（见第九章）。
- 管线自检（`阶段05-数据准备\数据集\_抽取缓存\v2.1\图谱管线\graph_check.json`，17 项）：通过 15 项。已通过的包括节点标签在本体内、节点编号唯一且非空（3507）、导出物中出现的关系名全部是冻结的 9 条之一（CUSTOMER_OF 因本版 0 条边而不在按关系汇总的列表里）、边端点全部存在（0 条悬空）、除 EVIDENCED_BY 外的 8 条关系的证据属性齐全（2089 条，缺失 0）、EVIDENCED_BY 终点全部是 Document（1644 条）、事件核心属性列与取值合法、role 在冻结枚举内、ISSUED_BY 只出现在政策与监管事件上（172 条）、发布方不同时又是参与方。
- 2 项**已知缺口**如实保留，不修数字、不编日期凑数：`event_time_nonempty`（989／1623 为空）与 `belongs_to_carries_validity_columns`（15 条 BELONGS_TO 无一带有效期）。两项都在第八章逐条说明。

### 6.3 四件导出物

| 文件 | 内容与规模 |
| --- | --- |
| `nodes.csv` | 3507 行（另表头 1 行）、33 列、891394 字节、sha256 `b9cdb0a2…`；按 node_id 确定性排序 |
| `edges.csv` | 3733 行、9 列、174477 字节、sha256 `695c21c6…`；按（关系, 起点, 终点, 证据块）确定性排序 |
| `graph_stats.json` | 各标签节点数、各关系边数、事件核心属性空值数、合并数、孤立节点数、跳过关系数与样本、生成时间与缓存指纹 |
| `replay.cypher` | 由导出物重建图谱的 Cypher：7 个标签的唯一性约束与 3 个索引，再按导出物写入节点与边；897076 字节 |

- 导出物字段：nodes.csv 33 列、edges.csv 9 列；**禁字段** `quote`／`quote_match_count`／`note`／`content`／`response_text` 一个都不在表头（`forbidden_export_fields`）——引文与正文一律不出仓库，导出物只留编号与结构化属性。
- 不复制正文：按 `body_text_check` 抽样 200 个文本块 × 3 段 × 100 字符的正文子串，在四个导出文件里 **0 命中**（种子 20260925）。
- Document 节点保留标题、来源、url、发布时间与类目，下游可从图谱直接回到原文（《10》第4.4.6节 的三级映射链路）。

### 6.4 时间属性的落点（《10》第4.5.6节）

- `event_time` → Event 节点属性；`publish_time` → Document 节点属性；`valid_from`／`valid_to` → BELONGS_TO 关系属性（本批 15 条全为空）。
- `data_cutoff_time` 是**版本级属性、不落图谱**：它属于数据集元信息（《10》第4.4.4节 与《02》第10.3节），图谱不重复存一份，避免同一时间事实出现两个可能不一致的落点。
- 相对时间（「近日」「本月」）**不折算成日期**：正文给不出可解析日期就写 null，绝不用发布时间顶替（《10》第4.5.5节 的时间归一化口径）。

### 6.5 重放与核对命令

- **幂等**：同输入重跑，nodes.csv／edges.csv／replay.cypher 逐字节一致；唯一每次变化的是 graph_stats.json 的 `generated_at` 与缓存指纹（`excluded_from_byte_compare`）。
- **重放零模型调用**：`--force=True --allow-api-calls=False`，上限 0 次，缓存缺一篇即硬失败（不降级、不编造）。
- 核对命令（在仓库根目录执行；路径含中文或空格时用引号包住）：
  - 重跑四环：`python "代码\抽取与图谱\run_all.py" --profile v21 --force --allow-api-calls=False`
  - 覆盖性重算：`python "代码\抽取与图谱\sample_eval_set.py" --verify-only`
  - 跨文档核验：`python "工具\跨文档核验.py"`
  - 阶段验收：`python "工具\验收第6阶段.py" --profile v21`
- 第三章的三条对账恒等式就是按上述命令的产物核对出来的，不是手抄。

## 七、评测集构成与分层依据

### 7.1 规模与两段式

- Dev **60** ＋ Test **200** ＝ **260** 条，单位是**文本块**（一条 = 一个文本块），占 5018 块的 5.2%（`分层统计.json` 的 `counts`）。
- chunk 交集 **0**（Dev 60 个与 Test 200 个互不相交）；**文档交集 0**：整篇文档只归一个 split，构造上不可能重叠（`document_disjoint_by_construction`），比「尽量不重叠」更强。
- 先选 Dev、再选 Test：Test 的候选池排除 Dev 已选文档；两段各自按类目配额与（类目 × 月份）单元格配额独立配平（`dev_test_split_rule`）。Test 只在规则与 Prompt 冻结后使用，不得回灌调参。
- 评测集落在 `阶段05-数据准备\数据集\抽取评测集\v2.1\`（`dev.jsonl`／`test.jsonl`／`标注说明.md`／`分层统计.json`），位于 `.gitignore` 覆盖的数据集目录之下，不入公开仓库。

### 7.2 分层配额（类目 × 月份 × 公司）

| 类目 | 合计 | Dev | Test |
| --- | --- | --- | --- |
| 公告 | 209 | 48 | 161 |
| 财经新闻 | 29 | 7 | 22 |
| 政策文件 | 15 | 3 | 12 |
| 监管公开信息 | 7 | 2 | 5 |
| 合计 | 260 | 60 | 200 |

- 单元格配额按（类目 × 月份）配平；最小落点：类目合计 ≥ 6、每个 split ≥ 1、每个非空单元格 ≥ 1（`min_per_category_total`／`min_per_category_split`／`month_cell_floor`）。当某类目在某段内的配额小于非空月份数时不启用月份保底（这是「监管公开信息 × 2026-09」这一格为 0 条的原因，见 第7.7节）。
- 公司维度按 subject_companies 口径的文本块关联量占比分配（分母是公司与条目的关联总数；一处多公司计多次）。
- 覆盖优先于比例：重大合同／产品／投资并购与多公司文档按最小落点强制纳入（`coverage_floors` 的 Dev／Test 两套下限），这是**有意偏离**，不得读成按比例分配的结果（见 第7.7节）。

### 7.3 分层容差与逐项差异登记

- 本阶段声明的**容差是 3.0 pp**，类目／月份／公司三个维度同值（取自 `分层统计.json` 的 `sampler.tolerance`）。
- 实测最大偏差：类目 0.1654 pp／月份 0.7931 pp／公司 1.2963 pp，**全部在容差内**（阈值 3.0 pp）。
- 逐项差异登记在 `achieved_vs_target` 段（每个键一条 `delta_pp`）；本文件不重复列出 105 家公司的全部偏差，只列最差的 5 项（见 7.4）。
- 三维度口径：类目／月份的分母是条目数 260；公司的分母是「公司与条目的关联总数」。

### 7.4 表 15-C／15-D／15-E 的脚本重算值（与《15》第2.2节 逐项对照）

- 表 15-C：`company_list ≥ 2` 命中 66 篇（9.3%）／`subject_companies ≥ 2` 命中 9 篇（1.3%），与《15》声明一致。
- 表 15-D（关键词命中篇数，代理指标，不是抽取结果）：

| 口径 | 重算命中篇数 | 《15》声明 |
| --- | --- | --- |
| 公告标题级：业绩 | 21 | 21 |
| 公告标题级：监管 | 1 | 1 |
| 公告标题级：股权 | 46 | 46 |
| 公告标题级：投资并购 | 1 | 1 |
| 公告标题级：重大合同 | 100 | 100 |
| 公告标题级：产品 | 102 | 102 |
| 公告标题级：政策 | 45 | 45 |
| 公告标题级：重大经营 | 60 | 60 |
| 正文级：SUPPLIES | 32 | 32 |
| 正文级：CUSTOMER_OF | 88 | 88 |
| 正文级：COMPETES_WITH | 9 | 9 |

- 表 15-E（文本块级）：公告 4042 块（80.6%）／财经新闻 563 块（11.2%）／政策文件 284 块（5.7%）／监管公开信息 129 块（2.6%），合计 5018 块；逐月 2026-06 1226／2026-07 937／2026-08 1620／2026-09 1235。
- 读法：表 15-D 的「监管」一行，公告标题级只命中 1 篇——监管类信号主要来自「监管公开信息」与「财经新闻」两个类目（全类目口径 22 篇）。表 15-C 与 表 15-D 必须分开读：前者是「同篇出现两家以上公司」的证据下限，后者是关键词命中的代理指标。
- 公司维度最差偏差：000651 为 −1.2963 pp／600104 为 −0.9421 pp／601668 为 −0.8119 pp／600521 为 ＋0.8047 pp／601390 为 −0.7798 pp。

### 7.5 逐类的候选条目（可标注池，不是标注结果）

事件类型（候选条目 Dev／Test／合计，末列是全类目关键词命中篇数）：

| 序号 | 事件类型 | Dev | Test | 合计 | 全类目命中篇 |
| --- | --- | --- | --- | --- | --- |
| 1 | 业绩 | 3 | 6 | 9 | 27 |
| 2 | 监管 | 2 | 6 | 8 | 22 |
| 3 | 股权 | 3 | 17 | 20 | 50 |
| 4 | 投资并购 | 1 | 2 | 3 | 3 |
| 5 | 重大合同 | 5 | 27 | 32 | 103 |
| 6 | 产品 | 6 | 20 | 26 | 102 |
| 7 | 政策 | 7 | 21 | 28 | 73 |
| 8 | 重大经营 | 3 | 15 | 18 | 62 |

核心关系（候选条目 Dev／Test／合计，末列是正文级关键词命中篇数）：

| 序号 | 关系 | Dev | Test | 合计 | 正文级命中篇 |
| --- | --- | --- | --- | --- | --- |
| 1 | BELONGS_TO | 9 | 31 | 40 | 235 |
| 2 | SUPPLIES | 0 | 9 | 9 | 32 |
| 3 | CUSTOMER_OF | 4 | 14 | 18 | 88 |
| 4 | COMPETES_WITH | 0 | 1 | 1 | 9 |
| 5 | HAS_EXECUTIVE | 40 | 136 | 176 | 561 |
| 6 | PARTICIPATES_IN | 10 | 26 | 36 | 177 |
| 7 | ISSUED_BY | 35 | 106 | 141 | 462 |
| 8 | RELATED_TO | 11 | 32 | 43 | 171 |
| 9 | EVIDENCED_BY | 0 | 0 | 0 | 0（结构性关系，由程序生成，不设标注条目） |

- 覆盖标签计数（Dev／Test／合计）：公司间关系关键词 4／13／17、严格多公司 2／7／9、多公司 7／20／27、重大合同 5／27／32、产品 6／20／26、投资并购 1／2／3。
- 多公司口径：`multi_company` 共 27 条（Dev 7／Test 20）、`strict_multi` 共 9 条（Dev 2／Test 7）。
- 候选条目是**按覆盖标签选出来的块**，不是「已经标注的实例」：选块规则为一篇文档只出 1 条，在该条覆盖标签的块级命中里取「代理关键词命中种数最多、其次 chunk_index 最小」的一块；没有标签命中块时取全篇同规则的一块（`chunk_selection_heuristic`）。**选块偏向信号最强的块，不是均匀随机，而且它不是标注。**

### 7.6 标注状态（本阶段只交池子，不交标注）

- 260 条的标注槽位**一律为空**：entities／events／relations／times／ontology_boundary_log 均为空列表、notes 为空串，状态是 `pending_human_annotation`（`annotation_emptiness`，checked_items 为 260、非空槽位为空）。
- 因此 8 种事件类型与 9 条核心关系在 **Dev＋Test 中都没有标注条目**，逐类按证据不足登记（见 第8.8节）。本文件在任何地方都不把「候选条目」写成「已有标注」。

### 7.7 已登记的八项偏离

- **公司零覆盖**：10 家公司在 260 条里为 0 条（601668／605287／300757／000034／002531／920019／002200／601567／002022／002090），是比例分配的固有结果，不是漏抽；逐家带目标关联量与目标条目数。
- **单元格零覆盖**：「监管公开信息 × 2026-09」这一格为 0 条（该段内该类目配额小于非空月份数时未启用月份保底）。
- **薄类覆盖**：投资并购（计划 3 条）与 COMPETES_WITH（计划 1 条）少于阈值 5，如实登记。
- **程序性偏离 5 项**：Dev／Test 切分规则、覆盖优先于比例、选块启发式、文档不重叠的构造方式、表 15-E 逐月值的更正，逐条见 `deviations_registry`。

## 八、已知限制与证据不足清单

本章是**如实登记**：每一条都给实测数字、口径出处与**对下游的后果**，不辩护、不淡化。本章与《15》第八节 的验收要求一一对应。

### 8.1 event_time 为空 989／1623（60.9%）

- 口径（《15》第十二节 的裁决，《10》第4.5.5节 的时间归一化）：正文给不出可解析日期就写 null，**绝不猜测、绝不用发布时间顶替**——用发布时间顶替等于往图上灌伪造时间。
- 实测构成：有可解析日期的 634 条（1623 − 989）；空的 989 条里只有 **100 条**属「写了时间但无法核实到日」（`event_time_unverified` 警告，如「近日」「本月」这类无法落到日的表述），其余 **889 条是正文根本没写日期**。因此这是**语料固有属性，不是日期门过严**：把日期门放宽也换不回这 889 条。
- 空值按事件类型（抽取层，分母为该类型的抽取条数）：重大经营 210／423、业绩 170／227、产品 161／172、监管 126／160、重大合同 114／165、股权 113／363、政策 69／93、投资并购 26／46。除股权与政策外，多数类型**过半为空**。
- **对第 10 阶段的后果（必须写进题量规划）**：时间过滤（D／E 组）可作用的工程量只有这有日期的 634 条（39.1%）；设计「某月之后」「某季度内」这类时间约束题目时，可选事件池显著小于全部事件，**不得假定事件普遍带时间**；时间类题目占比与按月的题量分布，都要按实测空值率规划，不能按理想假设规划。
- 同源影响：这一条也是管线自检中 `event_time_nonempty` 被判为已知缺口的原因（见 6.2）。

### 8.2 CUSTOMER_OF 在导出物中 0 条（冻结规则的正确结果，不是缺陷）

- 抽取到 **10 条**（`report_stats.relation_types.CUSTOMER_OF`）；导出物 **0 条**（`graph_stats.counts.edges_by_relation` 里没有这个键）。原因：这 10 条中每条的**至少一个对手方**不在 105 家冻结名单的书写面内——涉及 12 家名单外主体（中国移动通信有限公司、中国电信集团有限公司、中国电信股份有限公司、中国邮政速递物流股份有限公司、贵州时代矿业有限公司、中国中元国际工程有限公司、安徽元控储充新能源科技有限公司、芜湖联飞、浪潮、川环科技，以及两家印尼主体）。
- 冻结口径：《10》第4.5.4节 原文是「匹配失败进入待消歧列表，**人工确认后再写入图谱**」，策略常量取值 `pending_confirmation_exclude_from_graph`。因此未写入图谱，是**规则的正确结果**。
- 逐条可核对：`阶段06-事件抽取与知识图谱\图谱导出\v2.1\待人工确认清单.md` 列出 10 条关系的关系编号／文档号／证据块号／两端名称／置信度，以及 12 家待确认对手方与处理动作。
- **两件事必须分开写，不得混为一谈**：
  1. **评测集口径**：《15》第八节 关于「9 条关系要么在 Dev＋Test 有标注条目、要么登记证据不足」的要求，是针对**评测集**的；CUSTOMER_OF 在评测集里有候选条目（Dev 4／Test 14／合计 18，见 7.5），这项要求**在评测集口径下成立**。
  2. **导出物口径**：导出物里 CUSTOMER_OF 为 0 条，这个缺口**在本节如实登记**，不用评测集口径掩盖。
- CUSTOMER_OF：导出物 0 条（抽取 10 条，端点全部未消歧，未写入图谱）；本行是导出物口径的登记行，与上面第 2 点是同一件事。

### 8.3 BELONGS_TO 的 valid_from／valid_to 15／15 全为空

- 抽取侧 BELONGS_TO **42 条，无一条带语料给出的日期**（`report_stats.belongs_to_with_valid_range` 为 0）；导出物 15 条，有效期为空的是 **15 条**（`belongs_to_carries_validity_columns` 的 with_validity 为 0）。
- 空值来自上游：正文没有写出「何时起属于该行业」这样的区间，不是写入环节丢的。
- **不做发布时间兜底**：`belongs_to_validity_fallback` 为 null。有效期是「关系成立的时间」，用文档发布时间代替会造出语料没说的区间（《10》第4.5.6节 的四类时间落点）。
- 后果：第 10 阶段如有「某公司何时属于某行业」「按行业归属做时间切片」这类题目，只能按「关系无有效期」如实标注；下游也不得把 Document 的 publish_time 当作 BELONGS_TO 的有效期使用。这一条同时是管线自检 `belongs_to_carries_validity_columns` 被判为已知缺口的原因。

### 8.4 公司提及消歧率 39.9%、跳过 973 条边、551 个孤立节点、1135 条待消歧

- 数字：公司提及 753／1888 消歧（39.9%）；跳过 973 条边；孤立节点 551 个；待消歧 1135 条（`no_alias_match` 1127 条、`distinct_entity_marker` 8 条，见 4.4）。
- 关键判断：**1127 条与 105 家名单的书写面毫无包含关系**（既不相等、也不互相包含），属名单之外的主体；另 8 条包含某家公司的书写面但残余带区分性标记，按「母公司 ≠ 子公司」不合并。
- 因此这是**冻结名单封顶**的结果，不是消歧算法的新缺陷：名单只有 105 家，而语料提到的机构主体远多于此（仅 Institution 就抽出 914 条提及，Company 提及 1888 条）。放宽阈值只会把无关主体合并进来，误差更大。
- 后果：图谱只覆盖 104 个公司节点；供应商／客户／竞争关系网络因跳过 973 条边而不完整；551 个孤立节点在检索里不可达。
- 正确的改进路径（不在本阶段范围内）：扩名单（另开数据版本）或按 4.2 的只读纪律与 `阶段06-事件抽取与知识图谱\图谱导出\v2.1\待人工确认清单.md` 做人工确认；**不是**放宽匹配阈值。

### 8.5 拒绝 81 条的分布与含义

- 按对象类型分：关系类 67／事件类 9／实体类 5，合计 81，分布在 46 篇文档上；按原因的分布见 第2.4节 表（domain／range 不合法 21、引文定位不到 13、ISSUED_BY 用错事件类型 12、事件引用未解析 9、端点类型不匹配 8、实体引用未解析 8、引文跨块边界 7、关系名不在 8 类内 3）。
- 含义：拒绝发生在**本地校验**，被拒条目既不入图谱、也不被静默改写；每条都带拒绝原因与文档号，可逐条复核（`extracted.jsonl` 的 `counts.rejected` 与 `report_stats.rejection`）。
- `quote_spans_chunk_boundary` 7 条与 50 字块重叠的关系：相邻文本块重叠 50 字符，一句话恰好跨在块边界上时，模型可能在两块里各引到半句；这类引文无法指向**唯一**文本块，一律拒绝而**不做拼接**（宁可漏一条，也不让证据落点含糊）。`quote_not_in_document` 13 条同理（抄写走样或跨块拼接）。
- 规模判断：81／8468 ≈ **1.0%** 的条目被拒，比例低且原因可查。这既不是「模型乱抽」的证据，也不是可以忽略的噪声——它是本地校验在起作用。

### 8.6 SUPPLIES 3／COMPETES_WITH 1 仍然很薄

- 导出物 SUPPLIES **3 条**、COMPETES_WITH **1 条**；抽取侧也只有 38 条与 7 条（跳过 35 条与 6 条后剩 3 条与 1 条）。
- 与输入侧量级相符：表 15-D 的正文级命中是 SUPPLIES 32 篇、CUSTOMER_OF 88 篇、COMPETES_WITH 9 篇；可标注下限是「同篇出现两家以上公司」的 66 篇（严格口径 9 篇，表 15-C）。
- **不得写成「关系证据充足」**：这两条关系在语料里本就稀薄，评测集的候选条目也少（SUPPLIES 候选 9 条、COMPETES_WITH 候选 1 条，见 7.5）。第 10 阶段出题时只能把它们当**少量实例类别**对待；标注若判定无实例，按证据不足登记。
- 公司间关系的建边必须回到正文判定「同篇出现两家以上公司」；表 15-C 与 表 15-D 要分开读。

### 8.7 其他如实登记的边界

- 4 篇文档没有事件（doc_id 1133／2033／2064／2084）→ 没有 Document 节点，因此是 705 而不是 709（见 6.1）。
- 无参与主体的事件 388 条、事件时间无法核实 100 条、引文多解 415 条（都是警告，见 2.4 与 3.6）。
- 图谱机检 17 项通过 15 项，2 项已知缺口与本章 8.1／8.3 同源，不修数字、不编日期凑数。
- 评测集 260 条的标注槽位全空（7.6）：本阶段的「证据不足」是**评测集口径**的登记，不是标注结论；标注完成后必须按标注结果重跑该登记。
- 10 家公司在 260 条里为 0 条（7.7），是比例分配的固有结果，不是漏抽。
- 抽取侧的模型版本只能钉到模型 id（端点不返回 revision），可重放性由缓存承担（1.1）——换端点或换模型 id 会让旧缓存失效，这是抽取侧唯一的不可重放入口，也是本阶段唯一的对外部服务的依赖点。

### 8.8 证据不足登记（评测集口径：8 种事件类型 ＋ 9 条核心关系）

事件类型：Dev＋Test 的标注条目均为 0 条，逐类登记如下（候选条目数见 7.5）。

- 事件类型「业绩」：评测集 0 条标注条目（候选 9 条），按证据不足登记。
- 事件类型「监管」：评测集 0 条标注条目（候选 8 条），按证据不足登记。
- 事件类型「股权」：评测集 0 条标注条目（候选 20 条），按证据不足登记。
- 事件类型「投资并购」：评测集 0 条标注条目（候选 3 条，薄类），按证据不足登记。
- 事件类型「重大合同」：评测集 0 条标注条目（候选 32 条），按证据不足登记。
- 事件类型「产品」：评测集 0 条标注条目（候选 26 条），按证据不足登记。
- 事件类型「政策」：评测集 0 条标注条目（候选 28 条），按证据不足登记。
- 事件类型「重大经营」：评测集 0 条标注条目（候选 18 条），按证据不足登记。

核心关系：Dev＋Test 的标注条目均为 0 条，逐条登记如下。

- 关系「BELONGS_TO」：评测集 0 条标注条目（候选 40 条），按证据不足登记。
- 关系「SUPPLIES」：评测集 0 条标注条目（候选 9 条，薄类），按证据不足登记。
- 关系「CUSTOMER_OF」：评测集 0 条标注条目（候选 18 条），按证据不足登记。
- 关系「COMPETES_WITH」：评测集 0 条标注条目（候选 1 条，薄类），按证据不足登记。
- 关系「HAS_EXECUTIVE」：评测集 0 条标注条目（候选 176 条），按证据不足登记。
- 关系「PARTICIPATES_IN」：评测集 0 条标注条目（候选 36 条），按证据不足登记。
- 关系「ISSUED_BY」：评测集 0 条标注条目（候选 141 条），按证据不足登记。
- 关系「RELATED_TO」：评测集 0 条标注条目（候选 43 条），按证据不足登记。
- 关系「EVIDENCED_BY」：评测集 0 条标注条目（结构性关系，由程序按 doc_id 生成，不设标注条目，不需要登记）。

- 登记口径说明：本登记是**评测集口径**（Dev＋Test 有没有标注实例），与导出物口径（图谱里有多少条边）是两件事，见 8.2 的两点分离。标注完成后，本登记必须按标注结果重跑。

## 九、对下游的使用说明

### 9.1 给第 7 阶段（向量检索与图谱查询）

- 图谱是**证据的索引**，不是答案来源：每条边带 `source_doc_id` 与 `source_chunk_id`，可回到数据集与原文（三级映射链路见《10》第4.4.6节；图谱路径的存储形式与局限见《10》第4.4.7节）。
- **可直接用**：PARTICIPATES_IN（事件↔参与主体）、ISSUED_BY（政策／监管事件↔发布机构）、HAS_EXECUTIVE（公司↔高管）、BELONGS_TO（公司↔行业）、RELATED_TO（事件↔相关政策）。
- **谨慎使用**：SUPPLIES 只有 3 条、COMPETES_WITH 只有 1 条、CUSTOMER_OF 本版 0 条（见 8.6 与 8.2）；RELATED_TO 只表示「事件与某政策有关联」，把它读成因果关系是错的（《02》第5.3节：公开事实与模型分析必须区分）。
- **时间**：event_time 为空的 989 条可以做「无时间」的事实检索，但不能参与时间过滤，也不能把空值当成某个具体时间（见 8.1）。
- **覆盖边界**：551 个孤立节点与 1135 条待消歧实体在检索里不可达；要覆盖就先扩名单或按待人工确认清单做人工确认，不要用放宽阈值的方式绕过。
- **证据粒度**：一条 = 一个文本块（《02》第12.7节：同一个文本块只算一个证据）；同一事件的多篇证据文档会产生多条 EVIDENCED_BY，合并后的一个事件节点其证据是**并集**，不要把并集读成多个事件。
- **落点**：图谱落 Neo4j（按 `replay.cypher` 重建）与文件导出物（四件，不含正文、可随仓库提交）；MySQL 的六张表不变，本阶段不引入新表（《10》第4.4节 与 第4.5.3节）。

### 9.2 给第 10 阶段（抽取评测集与实验）

- 评测集在 `阶段05-数据准备\数据集\抽取评测集\v2.1\`：`dev.jsonl`（60 条）、`test.jsonl`（200 条）、`标注说明.md`、`分层统计.json`。先 Dev 后 Test，Test 只在规则与 Prompt 冻结后测一次。
- 题量规划按实测分层：类目／月份／公司的容差 3.0 pp（7.3）；事件类型与关系的候选条目按 7.5 的两张表；时间约束题按 event_time 空值率 **60.9%** 规划（8.1）；薄类与证据不足类别按 8.6 与 8.8 显式对待，不得按类均衡配题。
- **标注尚未开始**：260 条槽位全空（7.6），必须先完成人工标注才可能出 P／R／F1；本阶段不产出任何指标。
- 标注完成后必须重跑本阶段的证据不足登记（8.8），命令：
  - 重跑取样与重算：`python "代码\抽取与图谱\sample_eval_set.py"`
  - 只核对不重算：`python "代码\抽取与图谱\sample_eval_set.py" --verify-only`
- 若第 10 阶段需要「带时间的题目」，请先按 8.1 的空值率评估可用事件池，再决定题量；不要先定题量、再回头找事件。

### 9.3 通用纪律

- **编号不可跨版本混用**：MEVT（去重合并组）与 EVT（导出物事件）是两套编号（5.4）；换 Prompt 版本或改抽取配置会改变事件集合、进而改变 EVT 编号——重放旧结果必须用同一配置与同一缓存。
- **提交边界**：导出物四件可随仓库提交（不含正文、不含引文）；抽取缓存含第三方正文，**不得提交**（`.gitignore` 覆盖）。
- **密钥纪律**：只经环境变量注入；脚本与文档只写读取方式，不写取值、不落盘、不进日志。
- 《02》第12.4节 的七项中属答案生成侧的三项仍为 TBD，归第 8 阶段与预实验固化；本阶段的交付物不触碰、不代填。

## 修订记录

| 版本 | 日期 | 修订内容 |
| --- | --- | --- |
| v1.0 | 2026-09-26 | 首版。第 6 阶段交付文档，逐节对应《15》第八节 要求的 9 个必备小节：抽取口径与配置／Prompt 与规则设计／抽取结果统计／实体消歧实现／事件去重实现／图谱写入与导出／评测集构成与分层依据／已知限制与证据不足清单／对下游的使用说明。数字实测自 `代码\抽取与图谱\_全量\v2.1\report_stats.json`、`阶段06-事件抽取与知识图谱\图谱导出\v2.1\graph_stats.json`、`阶段05-数据准备\数据集\抽取评测集\v2.1\分层统计.json` 与整链缓存产物；口径引自《10》第4.5.1节～第4.5.6节、《15》第八节；第八章登记 6 项已知限制与 2 项边界（event_time 空值 989／1623、CUSTOMER_OF 导出物 0 条、BELONGS_TO 有效期全空、消歧率与跳过边、拒绝 81 条分布、薄类关系，外加文档数 705 与机检 2 项已知缺口）。 |
| v1.1 | 2026-09-26 | **T9 收口时的三处同步**（不改动本文的任何数字与结论）：**① 依据行同步**——本文所依据的《02》已按《15》第2.4节（二）在 第12.4节 追加注记并升为 **v2.8**（抽取所用的模型、模型版本与 Prompt 版本属上游数据生产参数，不并入该表的七项，七项的计数与取值均不变），本文文件头的依据行按版本链同步（编制时 v2.7 → 收口时 v2.8）。**② 试跑记录再生成**——`阶段06-事件抽取与知识图谱\_试跑_图谱管线\` 的 12 篇试跑产物（T4～T7）是按当时的消歧规则与别名数据生成的，此后消歧环节的别名数据补入了公司注册全称（见 4.2 的两个只读来源），旧产物与当前代码重放对不上（`python 工具\验收第6阶段.py` 默认 pilot profile 的 H2／H3／I2／I3／J2～J5 会因逐字节比对不一致而报错），故于当日在**零模型调用**（命中缓存 12 篇）下用 `python 代码\抽取与图谱\run_all.py --profile pilot --force` 重跑一遍试跑链，此后 pilot 与 v21 两个 profile 的验收均为 101／101 通过。**③ 索引登记**——本文与《15》登记进《00-项目总览与索引》（第三节 编号表 与 4.7）。 |
